{"id":"01KXKY4YE4DXPHYY727117E3FF","timestamp":"2026-07-15T22:25:23.396314Z","op":"create","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KXKY3XWKM1TRR9X0F4SQ0SF0\n+position_column: todo\n+position_ordinal: '8280'\n+title: Add .installing daemon state and extend binary lookup with installer search directories\n+---\n+## What\n+Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`:\n+\n+1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n+   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs\n+   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n+2. **Installer-aware binary lookup** — `LSPDaemon`'s `$PATH` check and spawn must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n+   - extend the lookup to search `$PATH` first, then the extra directories\n+   - when the binary resolves only via an extra directory, pass the **absolute path** to the connection factory instead of the bare command (`/usr/bin/env` accepts absolute paths, so `ProcessLanguageServerConnection` needs no change)\n+\n+## Acceptance Criteria\n+- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n+- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n+- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via its absolute path\n+- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n+\n+## Tests\n+- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, absolute-path spawn argument via the fake connection factory, and the still-notFound case\n+- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: todo\n-position_ordinal: '8280'\n-title: Add .installing daemon state and extend binary lookup with installer search directories\n----\n-## What\n-Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`:\n-\n-1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n-   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs\n-   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n-2. **Installer-aware binary lookup** — `LSPDaemon`'s `$PATH` check and spawn must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n-   - extend the lookup to search `$PATH` first, then the extra directories\n-   - when the binary resolves only via an extra directory, pass the **absolute path** to the connection factory instead of the bare command (`/usr/bin/env` accepts absolute paths, so `ProcessLanguageServerConnection` needs no change)\n-\n-## Acceptance Criteria\n-- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n-- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n-- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via its absolute path\n-- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n-\n-## Tests\n-- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, absolute-path spawn argument via the fake connection factory, and the still-notFound case\n-- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXKYHJE0G18Y4J2G5GGVC2ZE","timestamp":"2026-07-15T22:32:17.088249Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -3,28 +3,30 @@\n - claude-code\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n+- 01KXKY4J6DZ6M7ETN54TZ81EAW\n position_column: todo\n position_ordinal: '8280'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n ## What\n-Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`:\n+Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\n\n 1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n-   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs\n+   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\n    - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n-2. **Installer-aware binary lookup** — `LSPDaemon`'s `$PATH` check and spawn must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n-   - extend the lookup to search `$PATH` first, then the extra directories\n-   - when the binary resolves only via an extra directory, pass the **absolute path** to the connection factory instead of the bare command (`/usr/bin/env` accepts absolute paths, so `ProcessLanguageServerConnection` needs no change)\n+   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\n+2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n+   - search `$PATH` first, then the extra directories\n+   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\n\n ## Acceptance Criteria\n - [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n - [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n-- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via its absolute path\n+- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\n - [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n\n ## Tests\n-- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, absolute-path spawn argument via the fake connection factory, and the still-notFound case\n+- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\n - [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,30 +3,28 @@\n - claude-code\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-- 01KXKY4J6DZ6M7ETN54TZ81EAW\n position_column: todo\n position_ordinal: '8280'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n ## What\n-Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\n+Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`:\n\n 1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n-   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\n+   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs\n    - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n-   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\n-2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n-   - search `$PATH` first, then the extra directories\n-   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\n+2. **Installer-aware binary lookup** — `LSPDaemon`'s `$PATH` check and spawn must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n+   - extend the lookup to search `$PATH` first, then the extra directories\n+   - when the binary resolves only via an extra directory, pass the **absolute path** to the connection factory instead of the bare command (`/usr/bin/env` accepts absolute paths, so `ProcessLanguageServerConnection` needs no change)\n\n ## Acceptance Criteria\n - [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n - [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n-- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\n+- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via its absolute path\n - [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n\n ## Tests\n-- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\n+- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, absolute-path spawn argument via the fake connection factory, and the still-notFound case\n - [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n\n ## Workflow\n","transaction_id":null}
{"id":"01KXNW3DHXZPJE8JW85WTEMMQ5","timestamp":"2026-07-16T16:28:05.053521Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: todo\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8280'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n ## What\n","transaction_id":null}
{"id":"01KXNX5N07PM3DW9XGVM8WF9M2","timestamp":"2026-07-16T16:46:46.791633Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,22 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kxnx5n06ecdv5kwd578tsgah\n+  text: |-\n+    Implemented both parts of the task:\n+\n+    1. Added `LSPDaemonState.installing` case (Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift) with a doc comment on the enum noting the Codable-schema implication of adding a case. Added `LSPDaemon.noteInstalling()`: guarded to only transition from `.notFound`/`.notStarted`; any other current state is a no-op logged via `Log.lsp.fault` (chose the no-op+log guard over a hard `assert`/`assertionFailure`, specifically so tests can exercise the rejection path without crashing the test process). Updated `CodeContextState.isSettled`'s exhaustive switch (CodeContextState.swift) to treat `.installing` as not settled, alongside `.starting`/`.notStarted`/`.shuttingDown`. Confirmed via grep that `LSPDaemon.swift`, `LspSupervisor.swift`, and `CodeContextState.swift` are the only three production files that switch/pattern-match over `LSPDaemonState`; `LspSupervisor.swift`'s only match is a non-exhaustive `guard case .failed = ...`, so it needed no change.\n+\n+    2. Extended `BinaryLookup` (Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift) with a new `Location` enum (`.onPath` / `.extraSearchDirectory(absolutePath:)`) and `resolve(command:extraSearchDirectories:)`, which searches `$PATH` first (via the existing `isOnPath`) then each extra directory in order, expanding `~` via `NSString.expandingTildeInPath`. `LSPDaemon.start()` now calls `resolve` with `spec.installer?.extraSearchDirectories ?? []`, and when the binary is found only via an extra directory, builds a full-memberwise-init `ServerSpec` copy (new private static `spawnSpec(for:location:)` helper) whose `command` is the resolved absolute path, handing that copy to the connection factory. The daemon's own stored `spec` and `command()`/`ServerStatus.command` are untouched in every path (confirmed by a dedicated test asserting `daemon.command()` still returns the bare name after a successful extra-dir-resolved start).\n+\n+    Explicitly left out of scope (confirmed against the kanban board): wiring `LspSupervisor`'s health loop to actually call `noteInstalling()`/invoke `ServerInstaller` is task 01KXKY5MDHKKE49ZX9BA2H6BDJ (\"Wire auto-install into LspSupervisor...\"), which depends on this task and is still blocked/not started.\n+\n+    Tests added (TDD, written first): 8 new cases in LSPDaemonTests.swift (extra-search-directory resolution incl. absolute-path spec copy + bare-name command(), still-.notFound when missing everywhere, noteInstalling() transitions from .notStarted/.notFound, rejection from .running/.shuttingDown, forceRestart() recovery from .installing to both .running and .notFound outcomes), 1 new case in CodeContextStateTests.swift (`isReadyFalseWhenServerInstalling`), and a new `BinaryLookupTests` suite (5 cases: on-PATH, not-found-anywhere, found-in-extra-dir, PATH-takes-precedence, tilde-expansion) in ServerInstallerTests.swift.\n+\n+    Verification: `swift build` exit 0; `swift test` full suite 525/525 passing in 45 suites (up from the 511-test pre-task baseline, +14 new tests, zero failures). Adversarial double-check agent launched to review before handoff.\n+  timestamp: 2026-07-16T16:46:46.790884+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","reverse_patch":"--- original\n+++ modified\n@@ -1,22 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kxnx5n06ecdv5kwd578tsgah\n-  text: |-\n-    Implemented both parts of the task:\n-\n-    1. Added `LSPDaemonState.installing` case (Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift) with a doc comment on the enum noting the Codable-schema implication of adding a case. Added `LSPDaemon.noteInstalling()`: guarded to only transition from `.notFound`/`.notStarted`; any other current state is a no-op logged via `Log.lsp.fault` (chose the no-op+log guard over a hard `assert`/`assertionFailure`, specifically so tests can exercise the rejection path without crashing the test process). Updated `CodeContextState.isSettled`'s exhaustive switch (CodeContextState.swift) to treat `.installing` as not settled, alongside `.starting`/`.notStarted`/`.shuttingDown`. Confirmed via grep that `LSPDaemon.swift`, `LspSupervisor.swift`, and `CodeContextState.swift` are the only three production files that switch/pattern-match over `LSPDaemonState`; `LspSupervisor.swift`'s only match is a non-exhaustive `guard case .failed = ...`, so it needed no change.\n-\n-    2. Extended `BinaryLookup` (Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift) with a new `Location` enum (`.onPath` / `.extraSearchDirectory(absolutePath:)`) and `resolve(command:extraSearchDirectories:)`, which searches `$PATH` first (via the existing `isOnPath`) then each extra directory in order, expanding `~` via `NSString.expandingTildeInPath`. `LSPDaemon.start()` now calls `resolve` with `spec.installer?.extraSearchDirectories ?? []`, and when the binary is found only via an extra directory, builds a full-memberwise-init `ServerSpec` copy (new private static `spawnSpec(for:location:)` helper) whose `command` is the resolved absolute path, handing that copy to the connection factory. The daemon's own stored `spec` and `command()`/`ServerStatus.command` are untouched in every path (confirmed by a dedicated test asserting `daemon.command()` still returns the bare name after a successful extra-dir-resolved start).\n-\n-    Explicitly left out of scope (confirmed against the kanban board): wiring `LspSupervisor`'s health loop to actually call `noteInstalling()`/invoke `ServerInstaller` is task 01KXKY5MDHKKE49ZX9BA2H6BDJ (\"Wire auto-install into LspSupervisor...\"), which depends on this task and is still blocked/not started.\n-\n-    Tests added (TDD, written first): 8 new cases in LSPDaemonTests.swift (extra-search-directory resolution incl. absolute-path spec copy + bare-name command(), still-.notFound when missing everywhere, noteInstalling() transitions from .notStarted/.notFound, rejection from .running/.shuttingDown, forceRestart() recovery from .installing to both .running and .notFound outcomes), 1 new case in CodeContextStateTests.swift (`isReadyFalseWhenServerInstalling`), and a new `BinaryLookupTests` suite (5 cases: on-PATH, not-found-anywhere, found-in-extra-dir, PATH-takes-precedence, tilde-expansion) in ServerInstallerTests.swift.\n-\n-    Verification: `swift build` exit 0; `swift test` full suite 525/525 passing in 45 suites (up from the 511-test pre-task baseline, +14 new tests, zero failures). Adversarial double-check agent launched to review before handoff.\n-  timestamp: 2026-07-16T16:46:46.790884+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","transaction_id":null}
{"id":"01KXNXMDQ4A2R7VS8ZXJY40GAT","timestamp":"2026-07-16T16:54:50.852381Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -20,7 +20,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -20,7 +20,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","transaction_id":null}
{"id":"01KXNY9S64X1KBTQ3WMXYCP7D1","timestamp":"2026-07-16T17:06:30.724641Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -46,4 +46,13 @@\n - [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-07-16 11:55)\n+\n+Scope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\n+\n+- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`.\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\n+\n+(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -46,13 +46,4 @@\n - [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-07-16 11:55)\n-\n-Scope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\n-\n-- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`.\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\n-\n-(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNYB1658STP9N2PGWWZ7JXJ","timestamp":"2026-07-16T17:07:11.685843Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -20,7 +20,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -20,7 +20,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","transaction_id":null}
{"id":"01KXNYJPZMDAXKRGRM4QS0AWR8","timestamp":"2026-07-16T17:11:23.380176Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -17,6 +17,16 @@\n\n     Verification: `swift build` exit 0; `swift test` full suite 525/525 passing in 45 suites (up from the 511-test pre-task baseline, +14 new tests, zero failures). Adversarial double-check agent launched to review before handoff.\n   timestamp: 2026-07-16T16:46:46.790884+00:00\n+- actor: claude-code\n+  id: 01kxnyjpzkw1dpbmwjm19z5aj9\n+  text: |-\n+    Pulled back from review into doing to address 2 open review findings (case-let binding style):\n+\n+    1. CodeContextState.swift isSettled: `case let .failed(_, attempts):` -> `case .failed(_, let attempts):`\n+    2. LSPDaemon.swift spawnSpec(for:location:): `guard case let .extraSearchDirectory(absolutePath) = location else { return spec }` -> `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`\n+\n+    Both located by searching for the pattern text (review's cited line numbers were stale). Pure mechanical rewrite, no logic change. swift build exit 0; swift test 525/525 passing (unchanged from pre-fix baseline). Adversarial double-check agent launched to verify before handoff.\n+  timestamp: 2026-07-16T17:11:23.379347+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","reverse_patch":"--- original\n+++ modified\n@@ -17,16 +17,6 @@\n\n     Verification: `swift build` exit 0; `swift test` full suite 525/525 passing in 45 suites (up from the 511-test pre-task baseline, +14 new tests, zero failures). Adversarial double-check agent launched to review before handoff.\n   timestamp: 2026-07-16T16:46:46.790884+00:00\n-- actor: claude-code\n-  id: 01kxnyjpzkw1dpbmwjm19z5aj9\n-  text: |-\n-    Pulled back from review into doing to address 2 open review findings (case-let binding style):\n-\n-    1. CodeContextState.swift isSettled: `case let .failed(_, attempts):` -> `case .failed(_, let attempts):`\n-    2. LSPDaemon.swift spawnSpec(for:location:): `guard case let .extraSearchDirectory(absolutePath) = location else { return spec }` -> `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`\n-\n-    Both located by searching for the pattern text (review's cited line numbers were stale). Pure mechanical rewrite, no logic change. swift build exit 0; swift test 525/525 passing (unchanged from pre-fix baseline). Adversarial double-check agent launched to verify before handoff.\n-  timestamp: 2026-07-16T17:11:23.379347+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","transaction_id":null}
{"id":"01KXNYN1ST05J7C9D47PXYK3WN","timestamp":"2026-07-16T17:12:39.994476Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -34,35 +34,4 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\n-Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\n-\n-1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n-   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\n-   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n-   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\n-2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n-   - search `$PATH` first, then the extra directories\n-   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\n-\n-## Acceptance Criteria\n-- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n-- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n-- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\n-- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n-\n-## Tests\n-- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\n-- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-07-16 11:55)\n-\n-Scope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\n-\n-- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`.\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\n-\n-(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n+## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,4 +34,35 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n+## What\n+Two compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\n+\n+1. **New `LSPDaemonState.installing` case** — \"the server binary is missing and an auto-install attempt is in flight.\" Add:\n+   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\n+   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\n+   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\n+2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\n+   - search `$PATH` first, then the extra directories\n+   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\n+\n+## Acceptance Criteria\n+- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\n+- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\n+- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\n+- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\n+\n+## Tests\n+- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\n+- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-07-16 11:55)\n+\n+Scope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\n+\n+- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`.\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\n+\n+(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNZ3VVHPPD6XRKY910RY601","timestamp":"2026-07-16T17:20:45.425246Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -30,7 +30,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -30,7 +30,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","transaction_id":null}
{"id":"01KXNZ4KR5JDQ34K8BTND72SWX","timestamp":"2026-07-16T17:21:09.893961Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -34,4 +34,4 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n+## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecompute<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,4 +34,4 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecompute<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding.\n\\ No newline at end of file\n+## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNZ5MPBNYYYZ41DKD4Y0FPF","timestamp":"2026-07-16T17:21:43.627455Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -30,7 +30,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -30,7 +30,7 @@\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n","transaction_id":null}
{"id":"01KXNZC8MNKPVW7N7HBP14M32K","timestamp":"2026-07-16T17:25:20.661234Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -27,6 +27,15 @@\n\n     Both located by searching for the pattern text (review's cited line numbers were stale). Pure mechanical rewrite, no logic change. swift build exit 0; swift test 525/525 passing (unchanged from pre-fix baseline). Adversarial double-check agent launched to verify before handoff.\n   timestamp: 2026-07-16T17:11:23.379347+00:00\n+- actor: claude-code\n+  id: 01kxnzc8mmrcr87at2k4kkmzy4\n+  text: |-\n+    Pulled back from review into doing to address the remaining open review finding (2026-07-16 12:15): publishServers/publishIndexing duplication in CodeContextState.swift.\n+\n+    Extracted a shared private helper `publishAndRecomputeIsReady(_ update: @MainActor @Sendable () -> Void) async`, which runs `update()` on the main actor and then recomputes `isReady` from the now-mutated `self.indexing`/`self.servers`. Both `publishServers(_:)` and `publishIndexing(_:)` now reduce to a single-line call: `await publishAndRecomputeIsReady { self.servers = servers }` / `{ self.indexing = indexing }`. This is a genuine extraction (not cosmetic): the MainActor.run + recompute shape now exists exactly once instead of duplicated across the two methods.\n+\n+    Verification: swift build exit 0 (no new warnings; the one pre-existing warning is an unrelated mlx-swift dependency build-system artifact). swift test: 525/525 passing in 45 suites, unchanged from baseline. Adversarial double-check agent launched to verify the concurrency/isolation reasoning before handoff.\n+  timestamp: 2026-07-16T17:25:20.660629+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","reverse_patch":"--- original\n+++ modified\n@@ -27,15 +27,6 @@\n\n     Both located by searching for the pattern text (review's cited line numbers were stale). Pure mechanical rewrite, no logic change. swift build exit 0; swift test 525/525 passing (unchanged from pre-fix baseline). Adversarial double-check agent launched to verify before handoff.\n   timestamp: 2026-07-16T17:11:23.379347+00:00\n-- actor: claude-code\n-  id: 01kxnzc8mmrcr87at2k4kkmzy4\n-  text: |-\n-    Pulled back from review into doing to address the remaining open review finding (2026-07-16 12:15): publishServers/publishIndexing duplication in CodeContextState.swift.\n-\n-    Extracted a shared private helper `publishAndRecomputeIsReady(_ update: @MainActor @Sendable () -> Void) async`, which runs `update()` on the main actor and then recomputes `isReady` from the now-mutated `self.indexing`/`self.servers`. Both `publishServers(_:)` and `publishIndexing(_:)` now reduce to a single-line call: `await publishAndRecomputeIsReady { self.servers = servers }` / `{ self.indexing = indexing }`. This is a genuine extraction (not cosmetic): the MainActor.run + recompute shape now exists exactly once instead of duplicated across the two methods.\n-\n-    Verification: swift build exit 0 (no new warnings; the one pre-existing warning is an unrelated mlx-swift dependency build-system artifact). swift test: 525/525 passing in 45 suites, unchanged from baseline. Adversarial double-check agent launched to verify the concurrency/isolation reasoning before handoff.\n-  timestamp: 2026-07-16T17:25:20.660629+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","transaction_id":null}
{"id":"01KXNZG6MH4TA1YVYFBP440MDH","timestamp":"2026-07-16T17:27:29.681128Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -43,4 +43,4 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecompute<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding.\n\\ No newline at end of file\n+## What\\n\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecomputeIsReady<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication. — Fixed: extracted `private nonisolated func publishAndRecomputeIsReady(_ update: @MainActor @Sendable () -> Void) async`, which runs `update()` on the main actor then recomputes `isReady` from the post-mutation `indexing`/`servers`. Both `publishServers(_:)` and `publishIndexing(_:)` now call it with a one-line closure.\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding. — Fixed: same extraction as above; `publishIndexing(_:)` now reads `await publishAndRecomputeIsReady { self.indexing = indexing }`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -43,4 +43,4 @@\n position_ordinal: '80'\n title: Add .installing daemon state and extend binary lookup with installer search directories\n ---\n-## What\\n\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecomputeIsReady<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication. — Fixed: extracted `private nonisolated func publishAndRecomputeIsReady(_ update: @MainActor @Sendable () -> Void) async`, which runs `update()` on the main actor then recomputes `isReady` from the post-mutation `indexing`/`servers`. Both `publishServers(_:)` and `publishIndexing(_:)` now call it with a one-line closure.\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding. — Fixed: same extraction as above; `publishIndexing(_:)` now reads `await publishAndRecomputeIsReady { self.indexing = indexing }`.\n\\ No newline at end of file\n+## What\\nTwo compiler-driven extensions to the daemon layer, in `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift` and `Sources/FoundationModelsCodeContext/CodeContextState.swift`. (Depends on the ServerInstaller task: it extracts `isExecutableOnPath` into the shared lookup helper this task extends — do not refactor that function independently.)\\n\\n1. **New `LSPDaemonState.installing` case** — \\\"the server binary is missing and an auto-install attempt is in flight.\\\" Add:\\n   - an internal `LSPDaemon.noteInstalling()` transition (valid only from `.notFound`/`.notStarted`; document and guard) that the supervisor calls before kicking off an install, so the state is observable via `stateUpdates`/`ServerStatus` while the install runs. No `noteInstallFailed()` counterpart is needed: the supervisor exits `.installing` on BOTH outcomes by calling `forceRestart()`, whose re-run of `start()`'s lookup naturally lands `.running` (binary now present) or `.notFound` (still missing).\\n   - update **every** exhaustive switch over `LSPDaemonState` the compiler flags — most importantly `CodeContextState.isSettled`, where `.installing` is NOT settled (a workspace mid-install is not ready), matching how `.starting` is treated\\n   - doc-comment the Codable-schema implication on the enum: `LSPDaemonState`/`ServerStatus` are public `Codable`, and the new case changes the encoded schema — no decode site exists inside this repo, but external consumers decoding persisted `ServerStatus` values must be rebuilt against this version\\n2. **Installer-aware binary lookup** — the shared lookup helper (extracted by the ServerInstaller task) must also honor `spec.installer?.extraSearchDirectories` (with `~` expansion), because native global installs from `go install`/`rustup` land in `~/go/bin`/`~/.cargo/bin`, which are often not on `$PATH`:\\n   - search `$PATH` first, then the extra directories\\n   - **absolute-path spawn mechanism**: `ConnectionFactory` is `(ServerSpec, URL) → ConnectionHandle` and the production factory spawns `spec.command` — there is no separate command parameter. When the binary resolves only via an extra directory, hand the factory a **rebuilt `ServerSpec` copy** whose `command` is the absolute path (full memberwise init, including the new `installer` field). The daemon's own stored `spec` / `command()` MUST remain the bare name — it is the supervisor's dedupe and session-routing key and `ServerStatus.command`.\\n\\n## Acceptance Criteria\\n- [ ] `.installing` is observable through `ServerStatus`, and `CodeContextState.isReady` is false while any daemon is installing\\n- [ ] `noteInstalling()` from `.running` or `.shuttingDown` is rejected (no-op or assertion, per the guard chosen)\\n- [ ] A binary present only in an `extraSearchDirectories` dir (not `$PATH`) is found and spawned via a spec copy carrying the absolute path, while the daemon's `command()`/`ServerStatus.command` still reports the bare name\\n- [ ] A binary on neither `$PATH` nor extra dirs still lands in `.notFound` with the existing hint warning\\n\\n## Tests\\n- [ ] Extend `Tests/FoundationModelsCodeContextTests` daemon/state tests: `.installing` settledness in `CodeContextState` (isReady false mid-install), `noteInstalling()` transition guards, and lookup tests using a temp directory as `extraSearchDirectories` containing a fake executable (chmod +x) — assert resolution, the absolute-path spec copy received by the fake connection factory, the bare-name `command()`, and the still-notFound case\\n- [ ] `swift test` passes (the exhaustive-switch updates keep everything compiling)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 11:55)\\n\\nScope: commit `12f6c2e` (`git diff 12f6c2e~1..12f6c2e`), excluding the trailing kanban-sync commit.\\n\\n- [x] `Sources/FoundationModelsCodeContext/CodeContextState.swift:239` — Enum case binding in switch statement uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. Change to `case .failed(_, let attempts):`. — Fixed: line now reads `case .failed(_, let attempts):`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift:369` — Enum case binding uses `case let` before the pattern instead of binding each variable individually with `let` inside the pattern. This violates the idiom that case variables should be bound with their own `let` inside the pattern. Change to `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`. — Fixed: line now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n\\n(5 additional findings on `Tests/FoundationModelsCodeContextTests/LSPDaemonTests.swift` `case let .failed(...)` bindings were dropped: verified against parent commit `12f6c2e~1` — every cited occurrence is identical pre-existing test code, merely line-shifted by this commit's unrelated insertion earlier in the file. Per the blanket test-refactor exception, findings about pre-existing test code are not relayed.)\\n\\n## Review Findings (2026-07-16 12:15)\\n\\nScope: checkpoint commit `1210f37` (`git diff HEAD~1..HEAD`), re-review after fixing the 2 prior case-let binding style findings.\\n\\n- [x] Both prior findings confirmed fixed: `CodeContextState.swift` now reads `case .failed(_, let attempts):` and `LSPDaemon.swift` now reads `guard case .extraSearchDirectory(let absolutePath) = location else { return spec }`.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:107` — publishServers and publishIndexing are near-verbatim duplicates. Both follow the identical pattern: await MainActor.run { set property; self.isReady = Self.computeIsReady(...) }. They differ only in which property is set and parameter order passed to computeIsReady. When a fix is needed (e.g., a change to the recomputation pattern), it must be applied to both, creating a maintenance burden and risk of drift. Extract a shared helper method parameterized by the property setter and the updated value, e.g., `private nonisolated func publishAndRecompute<T>(_ value: T, setter: (T) -> Void, computeReady: () -> Bool)` or use a closure-based approach to reduce the duplication.\\n- [ ] `Sources/FoundationModelsCodeContext/CodeContextState.swift:125` — publishIndexing duplicates the pattern established in publishServers. Both methods contain near-identical MainActor.run blocks that set a property and recompute isReady. This duplication inflates the surface area for future changes. Consolidate into a single shared helper method that both publishServers and publishIndexing call, parameterizing the differences as described in the publishServers finding.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNZGCDDB1WSNR3BM63MNTKS","timestamp":"2026-07-16T17:27:35.597403Z","op":"update","item_id":"01KXKY4YE32JB9PP072FMDNQ9A","forward_patch":"--- original\n+++ modified\n@@ -36,6 +36,13 @@\n\n     Verification: swift build exit 0 (no new warnings; the one pre-existing warning is an unrelated mlx-swift dependency build-system artifact). swift test: 525/525 passing in 45 suites, unchanged from baseline. Adversarial double-check agent launched to verify the concurrency/isolation reasoning before handoff.\n   timestamp: 2026-07-16T17:25:20.660629+00:00\n+- actor: claude-code\n+  id: 01kxnzgcdc0wnsc07f93jtq4m3\n+  text: |-\n+    Adversarial double-check agent returned PASS: confirmed the extraction is genuine (not cosmetic), the MainActor.run ordering guarantees computeIsReady always reads post-mutation state, the @MainActor @Sendable closure-capture pattern matches the existing publishProjects/publishDiagnostics precedent, and no regressions — it independently re-ran swift build (exit 0) and swift test --filter \"CodeContextStateTests|ManagerStateTests\" (29/29 passing).\n+\n+    Checked off both open review findings on the card. All 4 review findings across both review rounds are now resolved. Leaving task in doing, ready for /review.\n+  timestamp: 2026-07-16T17:27:35.596689+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","reverse_patch":"--- original\n+++ modified\n@@ -36,13 +36,6 @@\n\n     Verification: swift build exit 0 (no new warnings; the one pre-existing warning is an unrelated mlx-swift dependency build-system artifact). swift test: 525/525 passing in 45 suites, unchanged from baseline. Adversarial double-check agent launched to verify the concurrency/isolation reasoning before handoff.\n   timestamp: 2026-07-16T17:25:20.660629+00:00\n-- actor: claude-code\n-  id: 01kxnzgcdc0wnsc07f93jtq4m3\n-  text: |-\n-    Adversarial double-check agent returned PASS: confirmed the extraction is genuine (not cosmetic), the MainActor.run ordering guarantees computeIsReady always reads post-mutation state, the @MainActor @Sendable closure-capture pattern matches the existing publishProjects/publishDiagnostics precedent, and no regressions — it independently re-ran swift build (exit 0) and swift test --filter \"CodeContextStateTests|ManagerStateTests\" (29/29 passing).\n-\n-    Checked off both open review findings on the card. All 4 review findings across both review rounds are now resolved. Leaving task in doing, ready for /review.\n-  timestamp: 2026-07-16T17:27:35.596689+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n - 01KXKY4J6DZ6M7ETN54TZ81EAW\n","transaction_id":null}
