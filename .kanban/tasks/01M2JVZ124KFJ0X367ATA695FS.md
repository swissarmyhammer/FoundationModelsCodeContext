---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m2kw1b7w0q3dtgy8wm3xaxas
  text: |
    ### research — what the change must say

    - The red command gives 4 matches now: `README.md:12` (`Codable & Sendable`), `plan.md:28` (`higher-level package`), `plan.md:32` (`Codable & Sendable`), `plan.md:48` (the "What we are NOT porting" row).
    - The public tool API is `CodeContextTools.make(context:includesSchemaInInstructions:)`, `CodeContextTools.toolNames` and `CodeContextTools.operationNames`. `make` gives `[any Tool]`. Each tool type is `internal`, so no document and no example can name one.
    - The op strings, from the three `operations()` functions, in schema order:
      - `code_search` (9): get symbol, search symbol, list symbol, get callgraph, get blastradius, grep code, search code, find duplicates, query ast.
      - `code_navigation` (10): get definition, get type_definition, get hover, get references, get implementations, get code_actions, get rename_edits, get inbound_calls, search workspace_symbol, get diagnostics.
      - `code_index` (4): get status, get lsp_status, rebuild index, detect projects.
    - The choice parameters and their names: `direction` (inbound, outbound, both), `kind` (function, method, type, other), `scope` (working, file, sha), `severity` (error, warning, information, hint), `layer` (treesitter, lsp, embedding, all).
    - `ToolSupport.normalizedChoiceName` makes a name lowercase and removes `_` and `-`, so each op string and each choice name ignores case, `_` and `-`.
    - `docs/` holds `language-servers.md` and `multiple-repos.md`. The new `docs/tools.md` goes beside them, and the README "Documentation" list links it.
    - The test finds the package root with `#filePath`, as `ConnectionTests` does for its scripted server.
  timestamp: 2026-09-16T01:06:57.404506+00:00
- actor: claude-code
  id: 01m2kw8v96vnw8nybgstys0zaa
  text: |
    ### implement — changed
    - evidence: 5 files. New: docs/tools.md, Tests/FoundationModelsCodeContextTests/ToolsDocumentationTests.swift. Changed: README.md, plan.md, Examples/CodeContextExample/main.swift.
    - TDD red step: with the test file in place and no document, `swift test --filter ToolsDocumentationTests` gave 3 failed tests and 5 issues (two "no such file" errors for docs/tools.md, and 3 missing tool names in README.md). After the documents, the same command gives 3 passed tests.
    - `docs/tools.md` holds one op table and one parameter table for each tool, the verb-alias table and the noun-alias table of each tool, and a "rules of a name" list with each rule the card asks for: case/`_`/`-` are ignored, the verb and the noun can be in the two orders, `file` and `path` are aliases, `line` and `character` are 0-based and `file` is relative to the root, `query ast` takes `astQuery`, `get callgraph` accepts a `<file>:<line>:<column>` locator, and the `severity` input takes a name while the output gives the LSP number.
    - The document names no internal type. It names only `CodeContextTools.make(context:)`, `toolNames` and `operationNames`.
    - `plan.md`: the "engine only" passage now says one main target with the engine and the three `OperationTool`s, and examples and integration tests as separate targets; `Codable & Sendable` is now `Encodable & Sendable`; the "What we are NOT porting" row now says the main target ships the three tools. Two more corrections keep the document true: the package-shape block now shows `Tools/`, `Examples/` and `IntegrationTests/`, and the Dependencies list now names FoundationModelsExtras, whose `Operations` product Package.swift gives to the main target.
    - `Examples/CodeContextExample/main.swift`: `import FoundationModels`, and after `start()` a loop that prints the name of each tool of `CodeContextTools.make(context:)` with its op strings from `operationNames`. It calls no language model.
    - The test finds the package root from `#filePath` with 3 `deletingLastPathComponent` steps, the same method as `ConnectionTests`. It reads the tables through `@testable import`, and holds the three tools in one table, so the three arms are data and not parallel code.
    - next: /test (full suite).
  timestamp: 2026-09-16T01:11:03.206875+00:00
- actor: claude-code
  id: 01m2kw8yk5cvfeezjbg07hfrg0
  text: |
    ### test — green
    - evidence: `swift test` exit 0 — 619 tests in 57 suites passed, 0 failed, 0 skipped. The log has no `error:` line and no `warning:` line. The test time was 4.7 seconds.
    - The count is 3 tests more than the 616 tests of the last task, because this task adds the 3 tests of ToolsDocumentationTests.
    - `swift build --target CodeContextExample` exit 0 (Build complete).
    - `swift format lint -r --strict Sources Tests Examples IntegrationTests Package.swift` exit 0.
    - `swift build --package-path IntegrationTests --build-tests` exit 0 (Build complete).
    - The acceptance command `rg -n "higher-level package|Codable & Sendable" plan.md README.md` gives no output (exit 1, no match).
    - next: /commit.
  timestamp: 2026-09-16T01:11:06.597923+00:00
depends_on:
- 01M2JVYP3GK00MFXF66DH7WM25
position_column: doing
position_ordinal: '80'
title: 'Docs and example: the package now ships FoundationModels tools'
---
## What
The user said: "update the plan -- we can have Tool code. This needs to be just one main target, examples and integration tests can be additional targets". The documents now say the opposite. Update them, document the three tools with their aliases, and show them in an example.

- [ ] `plan.md`: rewrite the passage that says the package is "engine only" and that "a higher-level package … wraps these ops as FoundationModels `Tool` implementations", and the "What we are NOT porting" table row that says "Consumer wraps ops as FoundationModels `Tool`s in a higher-level package". New design: one main target that contains the engine and three `OperationTool`s from FoundationModelsExtras; examples and integration tests are separate targets; still no server, no MCP and no CLI. Correct the claim that every op result is `Codable` (the results are `Encodable`). Find the text, not line numbers.
- [ ] `README.md`: rewrite the sentence that says each operation returns "a plain `Codable & Sendable` value" and that a FoundationModels `Tool` "is a thin layer over one async method". The new text says each result is `Encodable & Sendable`, and that the package ships three tools. Add a short "Tools" section with a code sample: `let tools = try CodeContextTools.make(context: context)` and `LanguageModelSession(tools: tools, instructions: ...)`.
- [ ] New `docs/tools.md`: a table for each tool (`code_search`, `code_navigation`, `code_index`) with each op string, its parameters and their aliases, and the tool's verb aliases and noun aliases. Add notes: op strings and parameter names ignore case, `_` and `-`, and the verb and noun can be in either order (so `get typedefinition` and `type_definition get` work); `file` and `path` are aliases; `line`/`character` are 0-based and `file` is relative to the root; `query ast` takes `astQuery`; `get callgraph` accepts a `<file>:<line>:<column>` locator; `severity` in `get diagnostics` output is the LSP number (1 = error, 2 = warning, 3 = information, 4 = hint) while the input uses names. Link it from `README.md`.
- [ ] `Examples/CodeContextExample/main.swift`: add `import FoundationModels`; after `start()`, build `CodeContextTools.make(context:)` and print each tool name with its op strings from `CodeContextTools.operationNames`. Do not call a language model.
- [ ] Write all new and changed document text in ASD-STE100 Simplified Technical English.

## Acceptance Criteria
- [ ] `rg -n "higher-level package" plan.md README.md` gives no output.
- [ ] `rg -n "Codable & Sendable" README.md plan.md` gives no output.
- [ ] Every op string in `CodeContextTools.operationNames` appears in `docs/tools.md`, and every tool name appears in `README.md`.
- [ ] `swift build --target CodeContextExample` exits 0.

## Tests
- [ ] New `Tests/FoundationModelsCodeContextTests/ToolsDocumentationTests.swift` (`@testable import FoundationModelsCodeContext`): read `docs/tools.md` and `README.md` from the package root (use `#filePath` to find it) and assert that each op string in `CodeContextTools.operationNames`, each verb-alias key and each noun-alias key of the three tools (`CodeSearchTool.verbAliases` and so on) appears in `docs/tools.md`, and that each name in `CodeContextTools.toolNames` appears in `README.md`.
- [ ] Red step: before the change, `rg -n "higher-level package|Codable & Sendable" plan.md README.md` finds matches; after the change, it gives no output.
- [ ] `swift build --target CodeContextExample` exits 0.
- [ ] `swift format lint -r --strict Sources Tests Examples IntegrationTests Package.swift` exits 0.
- [ ] `swift test` exits 0.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #tools #chore