---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m34f9vvrqbnehhcz1v4ex5wc
  text: |-
    Requests from foundationmodelsacpagent-08 (2026-09-22):

    1. Order of work: ship the log change first (the code and the message of `WireError.serverError`, one line for each (server, method) pair). Do not hold it for the capability work. If necessary, the capability work can land in a later commit.
    2. Acceptance check that session will run on main:
       - A Django clone under FoundationModelsACPAgent must not produce a flood of `prepareCallHierarchy failed` lines.
       - `get callgraph` on a Python symbol must give real callers, or an answer that says the server has no call hierarchy. It must not give an empty result that looks like "no callers".
    3. When the fix is on main, send the commit to foundationmodelsacpagent-08.
  timestamp: 2026-09-22T11:51:30.424694+00:00
- actor: claude-code
  id: 01m34fkp9czc8q21w5gc08xpyp
  text: |-
    Research done (implement step):

    - Live pylsp 1.14.0 check: the `initialize` capabilities have no `callHierarchyProvider`, no `workspaceSymbolProvider`, no `implementationProvider`. `referencesProvider` is present. `prepareCallHierarchy` gives -32601.
    - pylsp gives `documentSymbol` in the flat `SymbolInformation` form. The range starts at column 0 (the `def` keyword), not at the name. `references` at `0:0` gives `[]`. `references` at `0:4` (the name) gives the callers, also in files that are not open. Thus the references fallback must find the column of the name in the file text.
    - pylsp flat symbols include local variables (for example `x = helper()` in a function). The caller of a reference must be the narrowest CALLABLE symbol that holds the reference, not the narrowest symbol.
    - `WireError` has no `LocalizedError` conformance. Thus each `error.localizedDescription` log site shows only "error 1". A `LocalizedError` conformance corrects all log sites at one time.
    - `lsp_call_edges.file_path` is the file whose index pass wrote the edge ("usually the caller's file", see `Schema.LspCallEdges`). The references fallback writes edges INTO the symbols of the file that it indexes. Thus the owner file of such an edge is the callee file. `reverseEdgeFiles` looks only at `callee_id`; it must also look at `caller_id`, so that an owner file becomes dirty when a caller symbol in another file is deleted.
    - Plan: `ServerCapabilities` type decoded from `InitializeResult`; `LanguageServerConnection.initialize` returns it; `LSPDaemon` gives it to `LspSession`; `LspSession` does not send the three gated methods when the server does not advertise them; `LspSession` logs a failure one time for each (server, method) pair; `LSPIndexWorker` uses references when call hierarchy is absent; `LiveOpsExtended.inboundCalls` uses references when call hierarchy is absent.
  timestamp: 2026-09-22T11:56:52.396920+00:00
- actor: claude-code
  id: 01m34gg0mm616wrhy9d0wkq3sm
  text: |-
    Implementation landed (not committed), all targeted suites green:

    - `WireError` conforms to `LocalizedError`: a log line now shows "server error -32601: Method Not Found: ..." (was "WireError error 1").
    - `ServerCapabilities` (public, LSPTypes.swift) is decoded from the `initialize` result (`callHierarchyProvider`, `workspaceSymbolProvider`, `implementationProvider`; `true` or an options object = advertised). `LanguageServerConnection.initialize(rootURI:)` now returns it (public protocol change; the three test conformers are updated). `LSPDaemon` gives it and the server command to `LspSession`.
    - `LspSession` throws `LspSessionError.notAdvertised` (no request sent) for prepareCallHierarchy/outgoingCalls/incomingCalls, workspaceSymbols and implementations when not advertised. `LspSession.logFailure(of:context:error:)` logs one line for each (server, request) pair; the index worker uses it for every request failure.
    - References fallback: `Index/LSPIndexWorker+References.swift`. Without call hierarchy the worker asks `references` at the NAME of each callable (`Index/SymbolNameLocator.swift`; pylsp ranges start at `def`), gives each reference to the narrowest callable symbol of its file (other files: one documentSymbol per file per pass), and writes edges INTO the indexed file's symbols (owner = callee file). `reverseEdgeFiles` now checks both the callee and the caller side.
    - `LiveOpsExtended.inboundCalls`: without call hierarchy the live layer asks `references` and attributes each via the new `LayeredContext.enclosingCallable` (callable kinds only, so a pylsp local variable is never the caller); the index layer holds the fallback edges.
    - Known limit (documented in the extension doc): a fallback edge refreshes when the callee file is indexed again; a new call in another file shows in the index after that, but the live inbound-calls layer shows it at once.
    - Discovery: `FakeLanguageServerConnection` needed per-uri documentSymbols and per-position references scripting; added.
  timestamp: 2026-09-22T12:12:20.500189+00:00
- actor: claude-code
  id: 01m34gtb3k11c721y0m9s34d1q
  text: |-
    ### implement — changed

    - Server capabilities are kept from `initialize`. `LspSession` blocks prepareCallHierarchy, outgoing/incoming calls, implementation and workspace/symbol when the server does not advertise them.
    - When a server has no call hierarchy, the index and the live inbound-calls op get callers from `textDocument/references` (LSPIndexWorker+References.swift, SymbolNameLocator.swift).
    - `WireError` now has a LocalizedError description with the code and the message. The log has one line for each (server, method) failure.
    - plan.md and docs/language-servers.md record the capability gating.
    - Evidence: `swift build` clean. `swift test`: 671 tests in 62 suites passed. `swift test --package-path IntegrationTests`: 2 tests passed (LiveSourceKit and the new LivePylsp).
    - Follow-up: ^1bvthg6 (staleness of the fallback edges).

    next: /review
  timestamp: 2026-09-22T12:17:58.899754+00:00
- actor: claude-code
  id: 01m34h0894q7xdnj0ex9fjcp08
  text: |-
    ### test — green
    - evidence: swift test — 671 passed, 0 failed, 0 skipped; swift test --package-path IntegrationTests — 2 passed, 0 failed, 0 skipped; swift format lint -s -r Sources Tests — clean; swift format lint -s -r IntegrationTests/Tests — clean. One build-system note seen in both swift build and swift test for IntegrationTests: "warning: missing creator for mutated node: (.../mlx-swift_Cmlx.bundle/Contents/MacOS)". This is a llbuild diagnostic about the third-party mlx-swift dependency's binary bundle, not a Swift compiler warning, and not in any file this task changed.
    - next: ready for review.
  timestamp: 2026-09-22T12:21:12.612929+00:00
position_column: doing
position_ordinal: '80'
title: The LSP layer asks for methods the server does not have, and the log hides the answer
---
Source: a SWE-bench run of FoundationModelsACPAgent on 2026-09-21. Measured with pylsp on 2026-09-22.

## What happens

On a Django clone, the agent log has hundreds of error lines, one for each Python symbol:

    [FoundationModelsCodeContext:lsp] prepareCallHierarchy failed for django/utils/tree.py:make_hashable: The operation couldn't be completed. (FoundationModelsCodeContext.WireError error 1.)

`WireError error 1` is `serverError(code:message:)`. Thus the server sent an answer, but the code and the message do not get to the log. `Index/LSPIndexWorker.swift` logs `error.localizedDescription`. For an enum with associated values, this gives only the case index.

## Cause

`pylsp` (the server of `Languages/Python.swift`) does not have call hierarchy. Its `initialize` result has no `callHierarchyProvider`. The request gives this answer:

    {"error": {"code": -32601, "message": "Method Not Found: textDocument/prepareCallHierarchy"}}

The same is true for `workspace/symbol` and `textDocument/implementation`. pylsp answers `textDocument/references`, `definition`, `typeDefinition`, `hover` and `documentSymbol`.

`LSP/Wire.swift` states the design: "this package never gates behavior on server capabilities (plan.md: 'no capability gating')". `InitializeResult` is empty on purpose. Thus the index worker asks each server for call hierarchy, for each symbol of each file.

## Cost

- The call-edge index stays empty for Python. Thus `get callgraph`, `get inbound_calls` and `get blastradius` give no result on a Python repository. A caller cannot tell this from "no callers".
- The log has one error for each symbol. This hides the real errors near it.

## Reproduction (one second, no benchmark)

    printf 'def helper():\n    return 1\n\n\ndef main():\n    return helper()\n' > sample.py

Over stdio to `pylsp`, send `initialize`, `initialized`, `textDocument/didOpen`, then `textDocument/prepareCallHierarchy` at line 0, character 4. The answer is the -32601 error above. The `capabilities` of the `initialize` result do not have `callHierarchyProvider`.

## Proposed changes

1. Keep the `initialize` capabilities. Send a request for a method only when the server advertises it. This changes the "no capability gating" decision. Record the change in plan.md.
2. When the capability is absent, use the references path that `Ops/LiveOpsExtended.swift` already documents (`lspCallersOf` / `references`). Thus the callers of a Python symbol still have an answer.
3. Log the code and the message of `WireError.serverError`. Do not log only `localizedDescription`. Also, do not log one line for each symbol. Log one time for each (server, method) pair.

## Acceptance

- [x] `InitializeResult` keeps the server capabilities.
- [x] The index worker does not send `prepareCallHierarchy`, `workspace/symbol` or `implementation` to a server that does not advertise them.
- [x] On a Python repository, `get callgraph`, `get inbound_calls` and `get blastradius` give callers from the references fallback.
- [x] A `WireError.serverError` log line shows the code and the message.
- [x] The log has a maximum of one line for each (server, method) failure.
- [x] plan.md records the change of the "no capability gating" decision.
- [x] A test with the `sample.py` reproduction above.

## Note

FoundationModelsACPAgent pushed a workaround in swissarmyhammer/skills, branch `code-context`, commit 12faa4b. It tells the model which verbs pylsp does not have. That is not a fix of this package.