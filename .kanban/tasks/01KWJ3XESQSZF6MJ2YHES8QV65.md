---
comments:
- actor: wballard
  id: 01kwn45n7sg1h3mm34fy7p72pb
  text: |-
    Implemented. Sources/CodeContextKit/Ops/LayeredContext.swift (SourceLayer enum, LayeredSymbolInfo, LayeredChunkInfo, lsp_symbols/lsp_call_edges/ts_chunks query helpers, enrichLocation) + Sources/CodeContextKit/Ops/LiveOpsCore.swift (5 ops: definition, typeDefinition, hover, references, implementations, each cascading live session -> lsp index -> tree-sitter -> none). Also added 5 typed wrapper methods to LspSession.swift (definition/typeDefinition/hover/references/implementations) delegating to LanguageServerConnection, and 5 setter methods to Tests/.../Support/FakeLanguageServerConnection.swift mirroring the existing setDocumentSymbolsResult pattern.

    Deliberate divergences from the Rust reference (documented in LiveOpsCore's type doc comment):
    1. Both follower-router seams (LiveLspRouter/MultiLspRouter) removed per task instructions -- this Swift port has no cross-process leader/follower topology.
    2. All 5 ops cascade through all 4 layers uniformly, including typeDefinition and implementations -- the Rust reference restricts typeDefinition to live-LSP-only and skips the LSP-index layer for implementations. This port's schema draws no such distinction, and the task's acceptance criteria explicitly calls for a uniform "4 layers x 5 ops" test matrix.

    Tests/CodeContextKitTests/LayeredOpsTests.swift: 25 tests covering the full 4x5 cascade matrix, fall-through on induced live-LSP connection errors (definition->lspIndex, hover->treeSitter, references->none), and syncOpen-before-live-request ordering (verified via FakeLanguageServerConnection.calls index comparison + didOpen text content).

    swift test --filter LayeredOpsTests: 25/25 pass. Full swift test: 390/390 pass, zero warnings.
  timestamp: 2026-07-03T23:14:10.809261+00:00
- actor: wballard
  id: 01kwn4jkwcjzm5bmnvbkvmzzen
  text: |-
    really-done verification complete. Fresh `swift test --filter LayeredOpsTests`: 25/25 pass. Fresh `swift test` (full suite, 3 consecutive runs): 390/390 pass, zero warnings (one isolated flake in the pre-existing ConnectionTests suite on an unrelated run, matching that suite's own documented process-contention flakiness under load -- not part of this diff, not reproduced on 3 subsequent runs).

    Adversarial double-check verdict: PASS. Verified SQL column bindings against Migrations.swift, the from_ranges JSON round-trip between LSPIndexWorker.encodeFromRanges/TSCallGraph.writeEdge (writers) and LayeredContext.parseFromRanges (reader), the syncOpen-before-live-request and error-swallowing-falls-through contracts, and that the 25 tests assert substantively (not vacuously). Two minor non-blocking observations noted (a slightly loose `>= 1` assertion in one test, and one redundant-but-harmless extra query in indexedImplementations' tree-sitter fallback) -- neither warrants a revision.

    Task left in doing column per /implement workflow, ready for /review.
  timestamp: 2026-07-03T23:21:15.404264+00:00
- actor: wballard
  id: 01kwn6vw6fjx5fbgc44148kj89
  text: |-
    Round-1 review findings addressed (9 items in the 2026-07-03 18:26 section, all checked off with per-item notes on the task description):

    1. definition/typeDefinition duplication -- already resolved in the committed code (shared liveDefinition/indexedDefinition helpers with a method closure param); no change needed, verified by re-reading source + double-check agent.
    2. indexedDefinition/indexedHover duplicated control structure -- extracted `indexedLookup<T>(db:filePath:line:character:fromSymbol:fromChunk:)`, both now call it with per-branch closures.
    3. `documentURI` single-call-site wrapper -- inlined into `syncLiveDocument`, function removed.
    4. `implementations` sourceText inconsistency -- added `includeSource: Bool = false` param (matching definition/typeDefinition's pattern), threaded through live/lspIndex/treeSitter layers; 4 new TDD tests.
    5 & 6. SECURITY path traversal (readFileContents + document-URI construction) -- added `isSafeRelativePath(_:)` mirroring LSPIndexWorker's existing fix, applied once at the top of `readFileContents` (the sole gateway both call sites go through, since syncLiveDocument's URI-build only runs after readFileContents already succeeded). Verified with an actual TDD red/green cycle: temporarily reverted the guard, watched the new regression test fail (traversal file was actually opened and queried), restored the fix, watched it pass.
    7. parseFromRanges only tested with empty `[]` -- added `referencesDecodesNonEmptyFromRangesIntoCallSiteLocations` with real non-empty from_ranges data, asserting exact decoded coordinates.
    8. Missing fall-through tests for typeDefinition/implementations -- added both.
    9. Missing syncOpen-ordering tests for hover/typeDefinition/implementations -- added all three.

    Verification: `swift build` clean (no new warnings), `swift test` 401/401 passing across 3 consecutive full runs. Adversarial double-check agent (independent read of the diff) returned PASS, confirming the indexedLookup extraction preserves behavior, the includeSource threading is correct per-layer, the path-traversal fix is complete with no other unguarded call sites, and none of the new tests are vacuous.

    Task left in `doing` for the next `/review` pass.
  timestamp: 2026-07-04T00:01:15.983532+00:00
depends_on:
- 01KWJ3WNDEXN2N4BSACPC5H3J4
- 01KWJ3TB95F2J0CZYW17DCP9H8
position_column: doing
position_ordinal: '80'
title: Layered cascade and core live ops (definition/typeDefinition/hover/references/implementations)
---
## What
Create `Sources/CodeContextKit/Ops/LayeredContext.swift` + `LiveOpsCore.swift` — port of `layered_context.rs` with both follower router seams removed. Cascade per op: (1) live session (`syncOpen` current disk text + typed request) tagged `.liveLSP`; (2) persisted `lsp_symbols`/`lsp_call_edges` tagged `.lspIndex`; (3) tree-sitter chunks tagged `.treeSitter`; (4) empty result tagged `.none` — never an error for "no data". Implement the five core live ops: `definition`, `typeDefinition`, `hover`, `references`, `implementations`, each returning a `Codable & Sendable` result carrying `sourceLayer`.

## Acceptance Criteria
- [ ] With a live fake session, ops return `.liveLSP` results; with session nil but index populated, `.lspIndex`; index empty but chunks present, `.treeSitter`; all empty, `.none` with empty payload
- [ ] Live-layer errors (connection failure) fall through to the next layer rather than surfacing
- [ ] `syncOpen` is called with current disk content before every live request

## Tests
- [ ] `Tests/CodeContextKitTests/LayeredOpsTests.swift`: cascade matrix (4 layers × 5 ops) using fake session + seeded store; fall-through on induced live error; syncOpen ordering assertion
- [ ] Run `swift test --filter LayeredOpsTests` → all pass

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.

## Review Findings (2026-07-03 18:26)

Scope: HEAD~1..HEAD (commit e080f5f)

- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:116` — definition and typeDefinition (lines ~88–114 and ~116–142) are near-verbatim duplicates that differ only in which session method they pass to liveDefinition: `$0.definition(...)` vs `$0.typeDefinition(...)`. Both have identical signatures, identical cascade invocations, identical indexedLayers calls, and identical empty results. Two functions differing only by a single method name are one function with a parameter. Combine into a single function parameterized by which method to pass (e.g., accept a `method: (LspSession<Connection>, DocumentURI, Position) async throws -> [Location]` parameter), or create a private helper that both definition and typeDefinition call with their respective methods as closures. **DIVERGED — no change needed:** the committed code already extracts this via the shared private `liveDefinition`/`indexedDefinition` helpers (parameterized by a `method` closure for the live-layer difference); verified via re-reading the current source and confirmed by the double-check agent's independent read. The two remaining thin public wrappers (`definition`/`typeDefinition`) are the necessary distinct API entry points and can't be merged further without losing them.
- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:252` — indexedDefinition (lines ~252–290) and indexedHover (lines ~347–366) duplicate the identical "try layer 2, then layer 3, else fail" control structure: check lspSymbolAt, if found build and return, else check tsChunkAt, if found build and return, else return nil. The pattern is copied verbatim despite only processing details differing. Extract the "try layer 2, fall back to layer 3" pattern as a helper accepting closures for layer-specific processing, eliminating the duplicated control structure. **Fixed as suggested:** extracted `indexedLookup<T>(db:filePath:line:character:fromSymbol:fromChunk:)`; both `indexedDefinition` and `indexedHover` now call it with their own result-building closures, preserving identical behavior (verified by full test suite + double-check).
- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:496` — Single-call-site helper that wraps one operation without meaningful abstraction: `documentURI(rootDirectory:relativePath:)` is called exactly once from `syncLiveDocument` and merely wraps `DocumentURI(rootDirectory.appendingPathComponent(relativePath).absoluteString)`. The inline call site is clear enough to not require extraction. Inline the wrapper. Change `let uri = documentURI(rootDirectory: rootDirectory, relativePath: filePath)` to `let uri = DocumentURI(rootDirectory.appendingPathComponent(filePath).absoluteString)` in `syncLiveDocument`, and remove the `documentURI` function. **Fixed exactly as suggested.**
- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:553` — In `implementations`, the sourceText field of DefinitionLocation is populated inconsistently across the three layers. The tree-sitter layer includes chunk.text (line 553), but both the live layer (line 520 in liveImplementations) and LSP-index layer (line 547 in indexedImplementations) set sourceText to nil. This breaks the contract: callers receive different data depending on which layer answers. The behavior diverges from definition/typeDefinition, where sourceText is handled uniformly across all layers via the includeSource parameter. For consistency, always set sourceText to nil across all layers in implementations (option a: remove chunk.text from line 553, set `sourceText: nil`), OR conditionally include it uniformly via a parameter or constant. Option (a) is simpler: since implementations has no includeSource parameter, remove chunk.text and return nil for all layers. **DIVERGED — took option (b):** added an `includeSource: Bool = false` parameter to `implementations`, threaded through `liveImplementations`/`indexedImplementations`, gating `sourceText` uniformly via `readSourceRange`/`chunk.text` exactly like `definition`/`typeDefinition` already do, per the task's own instruction to match that established includeSource-flag pattern. Added 4 new tests (one per layer plus a default-omits-source regression test) via TDD.
- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:722` — Path traversal vulnerability: file path constructed from unvalidated relativePath parameter. Attacker-controlled paths containing '..' could traverse outside the workspace root (e.g., '../../../etc/passwd'). Validate that relativePath does not contain '..' or leading '/'. Example: guard !relativePath.contains(".."), !relativePath.hasPrefix("/") else { return nil }. **Fixed:** added a private `isSafeRelativePath(_:)` guard mirroring `LSPIndexWorker`'s existing fix for the identical issue (rejects leading `/`, leading `~`, or any `..` path component), applied at the top of `readFileContents` — the sole gateway every disk read in this type goes through. Kept as a duplicated small private guard in this file rather than extracting to a shared utility, per the finding's own stated acceptable fallback, since a shared-location extraction would touch files outside this task's scope (there's a separate backlog task, `01KWN2RKXKGMG4A0M6QG5QEVM1`, already tracking hoisting this pattern once a third worker needs it).
- [x] `Sources/CodeContextKit/Ops/LiveOpsCore.swift:745` — Path traversal vulnerability: document URI constructed from unvalidated relativePath parameter. Same traversal risk as readFileContents without path validation. Apply path validation: guard !relativePath.contains(".."), !relativePath.hasPrefix("/") else { fatalError or throw }. **Fixed by the same guard as the prior item:** `syncLiveDocument` calls the now-guarded `readFileContents` before ever constructing the `DocumentURI`, so an unsafe path returns `nil` and short-circuits before the URI line runs — one root-cause fix covers both the read path and the URI-construction path. Verified with a TDD red/green cycle: temporarily reverted the guard, confirmed the new regression test failed (live connection was reached and the traversal file was opened), then restored the fix and confirmed green. Also confirmed via the double-check agent that no other call site in this file builds a `DocumentURI` or resolves a path against `rootDirectory` outside this guarded gateway.
- [x] `Tests/CodeContextKitTests/LayeredOpsTests.swift:272` — The parseFromRanges decoder is added to LayeredContext.swift to parse LSP call-edge from_ranges JSON, but LayeredOpsTests never exercises it with real (non-empty) data. The test referencesReturnsLspIndexWhenSessionNilAndIndexPopulated inserts a call edge with default fromRanges: '[]', so the decoder only processes the trivial empty case. If the decoder has a bug parsing real ranges (e.g., off-by-one in indices, malformed JSON tolerance), or if the format specification changes, the test suite won't catch it because the non-empty path is never exercised. Add a test that creates a call edge with actual from_ranges data (e.g., `fromRanges: "[[0,5,0,11]]"`) and verifies the references query returns those decoded ranges correctly, confirming the decoder works with real data and the encoder–decoder pair is compatible. **Fixed:** added `referencesDecodesNonEmptyFromRangesIntoCallSiteLocations`, seeding `fromRanges: "[[1,4,1,11]]"` (deliberately narrower than and offset from the caller's own declared range) and asserting the decoded reference range's exact coordinates, so the test only passes if the JSON array-of-quads was actually decoded rather than falling back to the caller's whole-symbol range.
- [x] `Tests/CodeContextKitTests/LayeredOpsTests.swift:433` — All 5 ops (definition, typeDefinition, hover, references, implementations) implement identical live-layer error handling: catch exceptions in the live request and return nil to trigger fallthrough. Yet fall-through error behavior is tested explicitly only for definition (line 386), hover (line 404), and references (line 420). typeDefinition and implementations have no corresponding fall-through tests, leaving the shared error-handling invariant tested unevenly. Add explicit fall-through tests for typeDefinition and implementations to ensure the shared error-handling invariant is tested uniformly across all ops, matching the pattern of definition, hover, and references. **Fixed:** added `typeDefinitionFallsThroughToLspIndexOnInducedLiveError` and `implementationsFallsThroughToLspIndexOnInducedLiveError`, matching the existing pattern exactly.
- [x] `Tests/CodeContextKitTests/LayeredOpsTests.swift:487` — All 5 ops implement identical syncOpen-before-live-request ordering: each live op calls syncLiveDocument (which calls session.syncOpen) before issuing its typed live request. Yet syncOpen ordering is tested explicitly only for definition (line 437) and references (line 464). hover, typeDefinition, and implementations have no corresponding syncOpen tests, leaving the shared synchronization invariant tested unevenly. Add explicit syncOpen tests for hover, typeDefinition, and implementations to ensure the shared syncOpen ordering invariant is tested uniformly across all ops, matching the pattern of definition and references. **Fixed:** added `hoverSyncsCurrentDiskContentBeforeIssuingTheLiveRequest`, `typeDefinitionSyncsCurrentDiskContentBeforeIssuingTheLiveRequest`, and `implementationsSyncsCurrentDiskContentBeforeIssuingTheLiveRequest`, matching the existing pattern exactly.

Note: one engine-reported finding (a stylistic rewrite of an existing `case let .didOpen(...)` pattern match in `LayeredOpsTests.swift` into `case .didOpen(..., let text)`) was dropped per the review contract's blanket exception against refactoring existing test code — it changes no behavior and only restyles already-written test code.