{"id":"01KXKY4J6EZXHC5SYMKZJAY447","timestamp":"2026-07-15T22:25:10.862155Z","op":"create","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,33 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KXKY3XWKM1TRR9X0F4SQ0SF0\n+position_column: todo\n+position_ordinal: '8180'\n+title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n+---\n+## What\n+Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n+\n+- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n+- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n+- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach), kills the process on timeout and throws `CodeContextError.timeout`.\n+- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n+  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (reuse/extract `LSPDaemon.isExecutableOnPath` into a shared internal helper rather than duplicating it)\n+  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops\n+  - logs start/success/failure with the captured output tail via `Log.lsp`\n+\n+## Acceptance Criteria\n+- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n+- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n+- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n+- [ ] `ProcessInstallRunner` runs real commands and enforces its timeout\n+\n+## Tests\n+- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n+- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), and `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout` — no network, CI-safe\n+- [ ] `swift test --filter ServerInstallerTests` passes\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,33 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: todo\n-position_ordinal: '8180'\n-title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n----\n-## What\n-Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n-\n-- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n-- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n-- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach), kills the process on timeout and throws `CodeContextError.timeout`.\n-- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n-  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (reuse/extract `LSPDaemon.isExecutableOnPath` into a shared internal helper rather than duplicating it)\n-  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops\n-  - logs start/success/failure with the captured output tail via `Log.lsp`\n-\n-## Acceptance Criteria\n-- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n-- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n-- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n-- [ ] `ProcessInstallRunner` runs real commands and enforces its timeout\n-\n-## Tests\n-- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n-- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), and `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout` — no network, CI-safe\n-- [ ] `swift test --filter ServerInstallerTests` passes\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXKYGVV9M5Y9H7JM72WJ7YMW","timestamp":"2026-07-15T22:31:53.961029Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -11,11 +11,12 @@\n Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n\n - `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n+- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\n - `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n-- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach), kills the process on timeout and throws `CodeContextError.timeout`.\n+- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\n - `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n-  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (reuse/extract `LSPDaemon.isExecutableOnPath` into a shared internal helper rather than duplicating it)\n-  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops\n+  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\n+  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\n   - logs start/success/failure with the captured output tail via `Log.lsp`\n\n ## Acceptance Criteria\n@@ -22,11 +23,12 @@\n - [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n - [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n - [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n-- [ ] `ProcessInstallRunner` runs real commands and enforces its timeout\n+- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\n+- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\n\n ## Tests\n - [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n-- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), and `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout` — no network, CI-safe\n+- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\n - [ ] `swift test --filter ServerInstallerTests` passes\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -11,12 +11,11 @@\n Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n\n - `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n-- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\n - `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n-- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\n+- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach), kills the process on timeout and throws `CodeContextError.timeout`.\n - `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n-  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\n-  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\n+  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (reuse/extract `LSPDaemon.isExecutableOnPath` into a shared internal helper rather than duplicating it)\n+  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops\n   - logs start/success/failure with the captured output tail via `Log.lsp`\n\n ## Acceptance Criteria\n@@ -23,12 +22,11 @@\n - [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n - [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n - [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n-- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\n-- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\n+- [ ] `ProcessInstallRunner` runs real commands and enforces its timeout\n\n ## Tests\n - [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n-- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\n+- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), and `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout` — no network, CI-safe\n - [ ] `swift test --filter ServerInstallerTests` passes\n\n ## Workflow\n","transaction_id":null}
{"id":"01KXNGSD6E5Q2ACW7S20QYPMHY","timestamp":"2026-07-16T13:10:22.670425Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: todo\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -3,8 +3,8 @@\n - claude-code\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8180'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n ## What\n","transaction_id":null}
{"id":"01KXNHV14ZPSQDVNM41VYM0D8V","timestamp":"2026-07-16T13:28:44.447604Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,18 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kxnhv14vhvtp2hdnvsx1jqjs\n+  text: |-\n+    Implemented ServerInstaller engine per spec:\n+    - Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift: BinaryLookup (extracted from LSPDaemon's old private isExecutableOnPath), LspAutoInstall (public policy, isEnabled default true, timeout default 300s), InstallRunResult, InstallRunner protocol, ProcessInstallRunner (real Process spawn via /usr/bin/env, combined stdout+stderr capture with bounded tail buffer, timeout via injectable clock, prompt termination on both timeout and ambient-task cancellation via withTaskCancellationHandler, using a ResumeGuard to avoid double-resuming the completion continuation when the timeout-kill and the natural terminationHandler both race), and ServerInstaller actor (memoizes a Task<Bool,Never> per spec.command so concurrent/sequential callers dedupe to exactly one runner invocation).\n+    - Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift: removed the old private isExecutableOnPath, start() now calls BinaryLookup.isOnPath(spec.command).\n+    - Tests/FoundationModelsCodeContextTests/Support/FakeInstallRunner.swift: scripted actor fake with invocation recording, updateResult(_:), and a gate (closeGate/openGate) for concurrency tests.\n+    - Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift: 8 ServerInstaller cases (success, nonzero exit, throw, disabled policy, nil installer, missing tool, sequential dedupe, concurrent dedupe) + 4 ProcessInstallRunner integration cases (real exit 0 w/ combined-output capture, real exit 1, real timeout, real cancellation).\n+\n+    Verification: `swift build` clean; `swift test` -> 511 tests / 44 suites all passing, including the new 12 tests and the pre-existing LSPDaemonTests exercising the extracted BinaryLookup helper. Adversarial double-check review launched to confirm no concurrency/Sendable issues before handoff.\n+  timestamp: 2026-07-16T13:28:44.443671+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kxnhv14vhvtp2hdnvsx1jqjs\n-  text: |-\n-    Implemented ServerInstaller engine per spec:\n-    - Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift: BinaryLookup (extracted from LSPDaemon's old private isExecutableOnPath), LspAutoInstall (public policy, isEnabled default true, timeout default 300s), InstallRunResult, InstallRunner protocol, ProcessInstallRunner (real Process spawn via /usr/bin/env, combined stdout+stderr capture with bounded tail buffer, timeout via injectable clock, prompt termination on both timeout and ambient-task cancellation via withTaskCancellationHandler, using a ResumeGuard to avoid double-resuming the completion continuation when the timeout-kill and the natural terminationHandler both race), and ServerInstaller actor (memoizes a Task<Bool,Never> per spec.command so concurrent/sequential callers dedupe to exactly one runner invocation).\n-    - Sources/FoundationModelsCodeContext/LSP/LSPDaemon.swift: removed the old private isExecutableOnPath, start() now calls BinaryLookup.isOnPath(spec.command).\n-    - Tests/FoundationModelsCodeContextTests/Support/FakeInstallRunner.swift: scripted actor fake with invocation recording, updateResult(_:), and a gate (closeGate/openGate) for concurrency tests.\n-    - Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift: 8 ServerInstaller cases (success, nonzero exit, throw, disabled policy, nil installer, missing tool, sequential dedupe, concurrent dedupe) + 4 ProcessInstallRunner integration cases (real exit 0 w/ combined-output capture, real exit 1, real timeout, real cancellation).\n-\n-    Verification: `swift build` clean; `swift test` -> 511 tests / 44 suites all passing, including the new 12 tests and the pre-existing LSPDaemonTests exercising the extracted BinaryLookup helper. Adversarial double-check review launched to confirm no concurrency/Sendable issues before handoff.\n-  timestamp: 2026-07-16T13:28:44.443671+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNJ984K64YFV55S4VCRMH4A","timestamp":"2026-07-16T13:36:30.355995Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -13,6 +13,18 @@\n\n     Verification: `swift build` clean; `swift test` -> 511 tests / 44 suites all passing, including the new 12 tests and the pre-existing LSPDaemonTests exercising the extracted BinaryLookup helper. Adversarial double-check review launched to confirm no concurrency/Sendable issues before handoff.\n   timestamp: 2026-07-16T13:28:44.443671+00:00\n+- actor: claude-code\n+  id: 01kxnj984kcyssa2j64ra9jd2w\n+  text: |-\n+    Adversarial double-check (via really-done) returned REVISE with 3 findings; addressed all three:\n+    1. Tests/FoundationModelsCodeContextTests/LiveSourceKitTests.swift's `isSourceKitLSPOnPath` had a stale doc comment referencing the now-deleted `LSPDaemon.isExecutableOnPath` and duplicated the PATH-scan logic BinaryLookup was created to centralize — replaced its body with a direct `BinaryLookup.isOnPath(\"sourcekit-lsp\")` call and fixed the comment.\n+    2. Documented the accepted, narrow PID-reuse race in ProcessInstallRunner.run (the timeout task and the cancellation handler both call `kill(pid, SIGKILL)` unconditionally on a raw captured pid) — same accepted tradeoff ProcessLanguageServerConnection.close() already makes, now called out explicitly in the doc comment rather than left implicit.\n+    3. Tests/FoundationModelsCodeContextTests/Support/FakeInstallRunner.swift's `run()` now asserts via `precondition` if a second call arrives while already gated, so a future at-most-once regression fails loudly instead of silently hanging.\n+\n+    Re-verified after fixes: `swift build` clean, `swift test` -> 511/511 tests in 44 suites passing.\n+\n+    Task is green and complete: ServerInstaller actor, LspAutoInstall policy, InstallRunner/ProcessInstallRunner, and the shared BinaryLookup helper are all implemented per spec, with 12 new tests plus the fixes above. Leaving in doing for /review.\n+  timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -13,18 +13,6 @@\n\n     Verification: `swift build` clean; `swift test` -> 511 tests / 44 suites all passing, including the new 12 tests and the pre-existing LSPDaemonTests exercising the extracted BinaryLookup helper. Adversarial double-check review launched to confirm no concurrency/Sendable issues before handoff.\n   timestamp: 2026-07-16T13:28:44.443671+00:00\n-- actor: claude-code\n-  id: 01kxnj984kcyssa2j64ra9jd2w\n-  text: |-\n-    Adversarial double-check (via really-done) returned REVISE with 3 findings; addressed all three:\n-    1. Tests/FoundationModelsCodeContextTests/LiveSourceKitTests.swift's `isSourceKitLSPOnPath` had a stale doc comment referencing the now-deleted `LSPDaemon.isExecutableOnPath` and duplicated the PATH-scan logic BinaryLookup was created to centralize — replaced its body with a direct `BinaryLookup.isOnPath(\"sourcekit-lsp\")` call and fixed the comment.\n-    2. Documented the accepted, narrow PID-reuse race in ProcessInstallRunner.run (the timeout task and the cancellation handler both call `kill(pid, SIGKILL)` unconditionally on a raw captured pid) — same accepted tradeoff ProcessLanguageServerConnection.close() already makes, now called out explicitly in the doc comment rather than left implicit.\n-    3. Tests/FoundationModelsCodeContextTests/Support/FakeInstallRunner.swift's `run()` now asserts via `precondition` if a second call arrives while already gated, so a future at-most-once regression fails loudly instead of silently hanging.\n-\n-    Re-verified after fixes: `swift build` clean, `swift test` -> 511/511 tests in 44 suites passing.\n-\n-    Task is green and complete: ServerInstaller actor, LspAutoInstall policy, InstallRunner/ProcessInstallRunner, and the shared BinaryLookup helper are all implemented per spec, with 12 new tests plus the fixes above. Leaving in doing for /review.\n-  timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNJV9CK3MQ2F352YX7QMZVZ","timestamp":"2026-07-16T13:46:21.459517Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -27,7 +27,7 @@\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -27,7 +27,7 @@\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNJW1B9F46M2JEK36RKSKHR","timestamp":"2026-07-16T13:46:45.993645Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -56,4 +56,9 @@\n - [ ] `swift test --filter ServerInstallerTests` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-07-16 08:40)\n+\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\n","reverse_patch":"--- original\n+++ modified\n@@ -56,9 +56,4 @@\n - [ ] `swift test --filter ServerInstallerTests` passes\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-07-16 08:40)\n-\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNJX9CB6GQ9PXTW2EFR6WHW","timestamp":"2026-07-16T13:47:26.987117Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -27,7 +27,7 @@\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -27,7 +27,7 @@\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNKRDH3Q1RY4CB8CWZASNH2","timestamp":"2026-07-16T14:02:15.971143Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -31,34 +31,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\n-Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n-\n-- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n-- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\n-- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n-- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\n-- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n-  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\n-  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\n-  - logs start/success/failure with the captured output tail via `Log.lsp`\n-\n-## Acceptance Criteria\n-- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n-- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n-- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n-- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\n-- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\n-\n-## Tests\n-- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n-- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\n-- [ ] `swift test --filter ServerInstallerTests` passes\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-07-16 08:40)\n-\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\n-- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -31,4 +31,34 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\n\\ No newline at end of file\n+## What\n+Create `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\n+\n+- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\n+- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\n+- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\n+- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\n+- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\n+  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\n+  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\n+  - logs start/success/failure with the captured output tail via `Log.lsp`\n+\n+## Acceptance Criteria\n+- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\n+- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\n+- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\n+- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\n+- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\n+\n+## Tests\n+- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\n+- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\n+- [ ] `swift test --filter ServerInstallerTests` passes\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-07-16 08:40)\n+\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\n+- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\n","transaction_id":null}
{"id":"01KXNM0CA6HKHVGGP02WBCYCN4","timestamp":"2026-07-16T14:06:36.870324Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -25,6 +25,21 @@\n\n     Task is green and complete: ServerInstaller actor, LspAutoInstall policy, InstallRunner/ProcessInstallRunner, and the shared BinaryLookup helper are all implemented per spec, with 12 new tests plus the fixes above. Leaving in doing for /review.\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n+- actor: claude-code\n+  id: 01kxnm0ca52bwztbnr3rqeytkb\n+  text: |-\n+    Addressed both review findings by extracting genuinely shared abstractions:\n+\n+    1. `Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift` (new) — a `final class BoundedTailBuffer: @unchecked Sendable` parameterized by `maxChunks: Int`. Deleted the duplicated private `StderrTailBuffer` (was in ProcessLanguageServerConnection.swift, cap 20) and `InstallOutputTailBuffer` (was in ServerInstaller.swift, cap 40); both call sites now construct `BoundedTailBuffer(maxChunks: 20)` / `BoundedTailBuffer(maxChunks: 40)` respectively.\n+\n+    2. `Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift` (new) — a generic `final class ResumeOnce<Value: Sendable>: @unchecked Sendable` (lock + hasResumed flag) exposing `resume(with:) -> Bool`, `resume(returning:)`, `resume(throwing:)`. Deleted the duplicated private `ResumeGuard` in ServerInstaller.swift; `ProcessInstallRunner.run` now uses `ResumeOnce` directly. Went further than a single call-site swap: refactored `PendingRequestTable` (ProcessLanguageServerConnection.swift) to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing `register`/`resolve`/`resolveOrBuffer`/`failAll` through the same primitive instead of each hand-rolling its own resume call — both call sites now share one idempotent-resume implementation.\n+\n+    Updated two stale doc-comment cross-references naming the deleted `StderrTailBuffer` type (Sources/FoundationModelsCodeContext/Index/Store.swift's `GenerationCounter` doc, Tests/FoundationModelsCodeContextTests/CodeContextStateTests.swift's `ObservationFlag` doc) to cite `BoundedTailBuffer` instead.\n+\n+    Verification: `swift build` clean (exit 0). `swift test` → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Ran an adversarial double-check (via really-done) focused specifically on whether wrapping `PendingRequestTable`'s continuations in `ResumeOnce` changes the `resolve(id:with:)` Bool semantics `awaitResponse(id:)`'s timeout race depends on — confirmed identical behavior, since dictionary-removal already guarantees single ownership before any `resume` call in that table, so `ResumeOnce`'s own hasResumed flag is redundant-but-harmless there while being load-bearing at `ProcessInstallRunner`'s genuinely concurrent race. Double-check verdict: PASS, no blocking findings. Confirmed no stale references to the deleted `StderrTailBuffer`/`InstallOutputTailBuffer`/`ResumeGuard` names remain anywhere in Sources or Tests.\n+\n+    Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -25,21 +25,6 @@\n\n     Task is green and complete: ServerInstaller actor, LspAutoInstall policy, InstallRunner/ProcessInstallRunner, and the shared BinaryLookup helper are all implemented per spec, with 12 new tests plus the fixes above. Leaving in doing for /review.\n   timestamp: 2026-07-16T13:36:30.355660+00:00\n-- actor: claude-code\n-  id: 01kxnm0ca52bwztbnr3rqeytkb\n-  text: |-\n-    Addressed both review findings by extracting genuinely shared abstractions:\n-\n-    1. `Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift` (new) — a `final class BoundedTailBuffer: @unchecked Sendable` parameterized by `maxChunks: Int`. Deleted the duplicated private `StderrTailBuffer` (was in ProcessLanguageServerConnection.swift, cap 20) and `InstallOutputTailBuffer` (was in ServerInstaller.swift, cap 40); both call sites now construct `BoundedTailBuffer(maxChunks: 20)` / `BoundedTailBuffer(maxChunks: 40)` respectively.\n-\n-    2. `Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift` (new) — a generic `final class ResumeOnce<Value: Sendable>: @unchecked Sendable` (lock + hasResumed flag) exposing `resume(with:) -> Bool`, `resume(returning:)`, `resume(throwing:)`. Deleted the duplicated private `ResumeGuard` in ServerInstaller.swift; `ProcessInstallRunner.run` now uses `ResumeOnce` directly. Went further than a single call-site swap: refactored `PendingRequestTable` (ProcessLanguageServerConnection.swift) to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing `register`/`resolve`/`resolveOrBuffer`/`failAll` through the same primitive instead of each hand-rolling its own resume call — both call sites now share one idempotent-resume implementation.\n-\n-    Updated two stale doc-comment cross-references naming the deleted `StderrTailBuffer` type (Sources/FoundationModelsCodeContext/Index/Store.swift's `GenerationCounter` doc, Tests/FoundationModelsCodeContextTests/CodeContextStateTests.swift's `ObservationFlag` doc) to cite `BoundedTailBuffer` instead.\n-\n-    Verification: `swift build` clean (exit 0). `swift test` → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Ran an adversarial double-check (via really-done) focused specifically on whether wrapping `PendingRequestTable`'s continuations in `ResumeOnce` changes the `resolve(id:with:)` Bool semantics `awaitResponse(id:)`'s timeout race depends on — confirmed identical behavior, since dictionary-removal already guarantees single ownership before any `resume` call in that table, so `ResumeOnce`'s own hasResumed flag is redundant-but-harmless there while being load-bearing at `ProcessInstallRunner`'s genuinely concurrent race. Double-check verdict: PASS, no blocking findings. Confirmed no stale references to the deleted `StderrTailBuffer`/`InstallOutputTailBuffer`/`ResumeGuard` names remain anywhere in Sources or Tests.\n-\n-    Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNMFFAJKSH5CMZERK3RSDB9","timestamp":"2026-07-16T14:14:51.474803Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -42,7 +42,7 @@\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -42,7 +42,7 @@\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNMGEPG1EAA7SEAC6VQS5CK","timestamp":"2026-07-16T14:15:23.600345Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -46,4 +46,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -46,4 +46,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNMHN2J76FRW5FEPAPTS4TF","timestamp":"2026-07-16T14:16:02.898856Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -42,7 +42,7 @@\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -42,7 +42,7 @@\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNN9XVMS951HQ9NQSPS9CGA","timestamp":"2026-07-16T14:29:18.324712Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -40,6 +40,19 @@\n\n     Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n+- actor: claude-code\n+  id: 01kxnn9xvm52g3h9dhhb37194k\n+  text: |-\n+    Addressed the two 2026-07-16 09:09 review findings:\n+\n+    1. Extracted `ProcessInstallRunner.run(tool:arguments:timeout:)`'s timeout/termination-handler/cancellation-handler race coordination into a new private static `awaitCompletion(process:pid:timeout:clock:drainTask:tailBuffer:) async throws -> InstallRunResult`. `run` is now a linear spawn -> start drain task -> await completion sequence; the withTaskCancellationHandler/withCheckedThrowingContinuation/ResumeOnce/timeoutTask/terminationHandler nesting all now lives in `awaitCompletion`, with the exact same race semantics preserved (same ResumeOnce, same unconditional-kill-by-pid tradeoff, same onCancel behavior).\n+\n+    2. Extracted the duplicated POSIX read(2)/EINTR loop into a new shared `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift` (`ProcessUtilities.readChunk(from:bufferSize:) -> Data?`). `ProcessLanguageServerConnection`'s former private `readChunk(from:)` (used by both `runReaderLoop` and `runStderrDrainLoop`) is deleted; both now call the shared utility with the existing `readChunkSize` constant. `ProcessInstallRunner.drainOutput` (ServerInstaller.swift) now calls the same shared utility instead of its own hand-rolled read loop. Updated stale doc-comment cross-references to the deleted private method.\n+\n+    Verification: `swift build` clean (exit 0). `swift test` -> 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Adversarial double-check launched via really-done to confirm the extractions are genuine (not cosmetic) and no concurrency/Sendable issues were introduced; awaiting its verdict before final handoff.\n+\n+    Leaving in doing, ready for /review once double-check confirms.\n+  timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -40,19 +40,6 @@\n\n     Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T14:06:36.869904+00:00\n-- actor: claude-code\n-  id: 01kxnn9xvm52g3h9dhhb37194k\n-  text: |-\n-    Addressed the two 2026-07-16 09:09 review findings:\n-\n-    1. Extracted `ProcessInstallRunner.run(tool:arguments:timeout:)`'s timeout/termination-handler/cancellation-handler race coordination into a new private static `awaitCompletion(process:pid:timeout:clock:drainTask:tailBuffer:) async throws -> InstallRunResult`. `run` is now a linear spawn -> start drain task -> await completion sequence; the withTaskCancellationHandler/withCheckedThrowingContinuation/ResumeOnce/timeoutTask/terminationHandler nesting all now lives in `awaitCompletion`, with the exact same race semantics preserved (same ResumeOnce, same unconditional-kill-by-pid tradeoff, same onCancel behavior).\n-\n-    2. Extracted the duplicated POSIX read(2)/EINTR loop into a new shared `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift` (`ProcessUtilities.readChunk(from:bufferSize:) -> Data?`). `ProcessLanguageServerConnection`'s former private `readChunk(from:)` (used by both `runReaderLoop` and `runStderrDrainLoop`) is deleted; both now call the shared utility with the existing `readChunkSize` constant. `ProcessInstallRunner.drainOutput` (ServerInstaller.swift) now calls the same shared utility instead of its own hand-rolled read loop. Updated stale doc-comment cross-references to the deleted private method.\n-\n-    Verification: `swift build` clean (exit 0). `swift test` -> 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Adversarial double-check launched via really-done to confirm the extractions are genuine (not cosmetic) and no concurrency/Sendable issues were introduced; awaiting its verdict before final handoff.\n-\n-    Leaving in doing, ready for /review once double-check confirms.\n-  timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNP1J88P4GR8PHMEG3FHZDZ","timestamp":"2026-07-16T14:42:12.872721Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -55,7 +55,7 @@\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -55,7 +55,7 @@\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNP2VBPC7A995AYERS6GNJQ","timestamp":"2026-07-16T14:42:54.966212Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNP4D1BWYVEZYMZAZBYCZH5","timestamp":"2026-07-16T14:43:45.835309Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -55,7 +55,7 @@\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -55,7 +55,7 @@\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNQ6W2FZHW948WVDZ8WWSP7","timestamp":"2026-07-16T15:02:35.343974Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -59,4 +59,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNQ785RPSKQ4RF69F7KQH39","timestamp":"2026-07-16T15:02:47.736896Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -53,6 +53,21 @@\n\n     Leaving in doing, ready for /review once double-check confirms.\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n+- actor: claude-code\n+  id: 01kxnq785r484n26e1dy580p21\n+  text: |-\n+    Addressed all 4 findings from the 2026-07-16 09:34 review round with a root-cause pass across both files (not just the cited lines):\n+\n+    1+4. Added `ProcessUtilities.envExecutablePath` (\"/usr/bin/env\") and `ProcessUtilities.defaultChunkSize` (65536) as shared constants in Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift. ProcessLanguageServerConnection's init and ServerInstaller's ProcessInstallRunner.run both now build their Process.executableURL from the shared constant; runReaderLoop/runStderrDrainLoop use ProcessUtilities.defaultChunkSize (the old private `readChunkSize` constant is deleted) and ServerInstaller's drainOutput no longer hardcodes 65536.\n+\n+    2. Extracted the full read-decode-append *loop* (not just the raw read) into ProcessUtilities.drainChunks(from:bufferSize:onChunk:), which loops on readChunk until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure. ProcessLanguageServerConnection.runStderrDrainLoop now calls it with a log-then-append closure; ServerInstaller's drainOutput calls it with an append-only closure. Both former hand-rolled while-loops are gone.\n+\n+    3. Restructured ProcessInstallRunner.awaitCompletion by extracting two new private static helpers: scheduleTimeout(pid:timeout:clock:resumeGuard:) and installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:). awaitCompletion's continuation body now just builds resumeGuard and calls those two helpers, so its own nesting is withTaskCancellationHandler -> continuation (2 levels) instead of 4; the timeout-task and termination-handler setup each live in their own shallow function.\n+\n+    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Grepped both files for remaining \"/usr/bin/env\" / \"65536\" literals -> none. Noted (but left untouched, out of scope) that Diagnostics/DiagnosticsScope.swift's unrelated git-subprocess helper also hardcodes \"/usr/bin/env\" independently -- not cited in this review round and not part of the LSP process-spawning pair these findings target.\n+\n+    All 4 findings checked off on the task. Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T15:02:47.736350+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -53,21 +53,6 @@\n\n     Leaving in doing, ready for /review once double-check confirms.\n   timestamp: 2026-07-16T14:29:18.324235+00:00\n-- actor: claude-code\n-  id: 01kxnq785r484n26e1dy580p21\n-  text: |-\n-    Addressed all 4 findings from the 2026-07-16 09:34 review round with a root-cause pass across both files (not just the cited lines):\n-\n-    1+4. Added `ProcessUtilities.envExecutablePath` (\"/usr/bin/env\") and `ProcessUtilities.defaultChunkSize` (65536) as shared constants in Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift. ProcessLanguageServerConnection's init and ServerInstaller's ProcessInstallRunner.run both now build their Process.executableURL from the shared constant; runReaderLoop/runStderrDrainLoop use ProcessUtilities.defaultChunkSize (the old private `readChunkSize` constant is deleted) and ServerInstaller's drainOutput no longer hardcodes 65536.\n-\n-    2. Extracted the full read-decode-append *loop* (not just the raw read) into ProcessUtilities.drainChunks(from:bufferSize:onChunk:), which loops on readChunk until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure. ProcessLanguageServerConnection.runStderrDrainLoop now calls it with a log-then-append closure; ServerInstaller's drainOutput calls it with an append-only closure. Both former hand-rolled while-loops are gone.\n-\n-    3. Restructured ProcessInstallRunner.awaitCompletion by extracting two new private static helpers: scheduleTimeout(pid:timeout:clock:resumeGuard:) and installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:). awaitCompletion's continuation body now just builds resumeGuard and calls those two helpers, so its own nesting is withTaskCancellationHandler -> continuation (2 levels) instead of 4; the timeout-task and termination-handler setup each live in their own shallow function.\n-\n-    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Grepped both files for remaining \"/usr/bin/env\" / \"65536\" literals -> none. Noted (but left untouched, out of scope) that Diagnostics/DiagnosticsScope.swift's unrelated git-subprocess helper also hardcodes \"/usr/bin/env\" independently -- not cited in this review round and not part of the LSP process-spawning pair these findings target.\n-\n-    All 4 findings checked off on the task. Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T15:02:47.736350+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNQFYD5YZ44TK63A8Y47GT5","timestamp":"2026-07-16T15:07:32.645336Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -68,6 +68,19 @@\n\n     All 4 findings checked off on the task. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T15:02:47.736350+00:00\n+- actor: claude-code\n+  id: 01kxnqfyd4xcpb75mbx33cz6ch\n+  text: |-\n+    Adversarial double-check (via really-done) returned REVISE with 2 findings; both are addressed with logged justification rather than code changes, per really-done's contract:\n+\n+    1. It flagged a +1 line change to `.github/workflows/ci.yml` (`example-targets: CodeContextExample ManagerExample`) as scope drift bundled into this diff. Verified via `git status`/`git diff` this modification predates this session entirely — I never touched `.github/workflows/ci.yml` in this task, and `git log` shows it wasn't part of any commit I'm aware of either; it's a pre-existing uncommitted change already sitting in the working tree unrelated to ServerInstaller/ProcessUtilities/ProcessLanguageServerConnection. Leaving it as-is: it's out of scope for this task and not something to revert on this task's behalf (that would risk discarding someone else's in-progress work).\n+\n+    2. It suggested a dedicated `ProcessUtilitiesTests.swift` for the new `drainChunks(from:bufferSize:onChunk:)`, specifically around the empty-chunk-skip behavior it introduces (the old `ServerInstaller.drainOutput` didn't check `!text.isEmpty` before appending). The double-check's own analysis confirmed this is not an observable behavior change: `readChunk` only returns `Data` when `bytesRead > 0`, and decoding non-empty valid UTF-8 bytes never yields an empty `String`, so the new guard is unreachable dead-code-equivalent in practice, not a functional regression. `drainChunks` is already exercised indirectly through `ConnectionTests` (stderr draining) and `ServerInstallerTests`/`ProcessInstallRunnerTests` (combined-output draining), consistent with how the underlying `readChunk` itself has never had its own dedicated unit test file. Judged this indirect coverage sufficient; not adding a new test file for this refactor-only round.\n+\n+    Re-verified after logging these: `swift build` clean (exit 0), `swift test` (full suite) -> 511/511 tests in 44 suites passing.\n+\n+    Task remains green, all 4 findings from the 2026-07-16 09:34 review round fixed and checked off. Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -68,19 +68,6 @@\n\n     All 4 findings checked off on the task. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T15:02:47.736350+00:00\n-- actor: claude-code\n-  id: 01kxnqfyd4xcpb75mbx33cz6ch\n-  text: |-\n-    Adversarial double-check (via really-done) returned REVISE with 2 findings; both are addressed with logged justification rather than code changes, per really-done's contract:\n-\n-    1. It flagged a +1 line change to `.github/workflows/ci.yml` (`example-targets: CodeContextExample ManagerExample`) as scope drift bundled into this diff. Verified via `git status`/`git diff` this modification predates this session entirely — I never touched `.github/workflows/ci.yml` in this task, and `git log` shows it wasn't part of any commit I'm aware of either; it's a pre-existing uncommitted change already sitting in the working tree unrelated to ServerInstaller/ProcessUtilities/ProcessLanguageServerConnection. Leaving it as-is: it's out of scope for this task and not something to revert on this task's behalf (that would risk discarding someone else's in-progress work).\n-\n-    2. It suggested a dedicated `ProcessUtilitiesTests.swift` for the new `drainChunks(from:bufferSize:onChunk:)`, specifically around the empty-chunk-skip behavior it introduces (the old `ServerInstaller.drainOutput` didn't check `!text.isEmpty` before appending). The double-check's own analysis confirmed this is not an observable behavior change: `readChunk` only returns `Data` when `bytesRead > 0`, and decoding non-empty valid UTF-8 bytes never yields an empty `String`, so the new guard is unreachable dead-code-equivalent in practice, not a functional regression. `drainChunks` is already exercised indirectly through `ConnectionTests` (stderr draining) and `ServerInstallerTests`/`ProcessInstallRunnerTests` (combined-output draining), consistent with how the underlying `readChunk` itself has never had its own dedicated unit test file. Judged this indirect coverage sufficient; not adding a new test file for this refactor-only round.\n-\n-    Re-verified after logging these: `swift build` clean (exit 0), `swift test` (full suite) -> 511/511 tests in 44 suites passing.\n-\n-    Task remains green, all 4 findings from the 2026-07-16 09:34 review round fixed and checked off. Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNR7J6P00ZAM34SAKXHJ03D","timestamp":"2026-07-16T15:20:26.582022Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -83,7 +83,7 @@\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -83,7 +83,7 @@\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNR9CH9VHZG939WD97JSXS2","timestamp":"2026-07-16T15:21:26.313557Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:507` — resolveCodeAction lacks a doc comment.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:507` — resolveCodeAction lacks a doc comment.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNRBFZS4HYRN0T8KHA5XHM8","timestamp":"2026-07-16T15:22:35.385712Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:507` — resolveCodeAction lacks a doc comment.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:507` — resolveCodeAction lacks a doc comment.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNRDNRW1V69XYSJEDDTE69F","timestamp":"2026-07-16T15:23:46.844764Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -83,7 +83,7 @@\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -83,7 +83,7 @@\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNTQA3M91CD074J29CYAW7H","timestamp":"2026-07-16T16:03:59.732816Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -87,4 +87,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNTQKED3YX4BVJ2A6XB6RVY","timestamp":"2026-07-16T16:04:09.293676Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -81,6 +81,19 @@\n\n     Task remains green, all 4 findings from the 2026-07-16 09:34 review round fixed and checked off. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n+- actor: claude-code\n+  id: 01kxntqkec7n87hbykk45k3ym6\n+  text: |-\n+    Addressed all 23 findings from the 2026-07-16 10:11 review round:\n+\n+    1. Extracted `ProcessLanguageServerConnection.awaitResponse(id:)`'s 4-level closure nesting (withThrowingTaskGroup -> group.addTask -> withCheckedThrowingContinuation -> body) into two private static helpers, mirroring the ServerInstaller.scheduleTimeout/installTerminationHandler split: `awaitRegisteredResponse(id:pendingRequests:)` wraps the continuation-registration side, `failAfterTimeout(id:timeout:clock:pendingRequests:)` wraps the sleep-then-resolve-or-cancel side. `awaitResponse` itself is now a linear task-group setup with two single-call `group.addTask` bodies.\n+\n+    2. Added a one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (initialize through pullDiagnostics), matching the file's house style for simple passthroughs -- a concise summary sentence, no `- Parameter`/`- Throws` boilerplate since the full contract already lives on `LanguageServerConnection`'s protocol declarations in LanguageServerConnection.swift.\n+\n+    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly.\n+\n+    All 23 findings checked off on the task. Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -81,19 +81,6 @@\n\n     Task remains green, all 4 findings from the 2026-07-16 09:34 review round fixed and checked off. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T15:07:32.644769+00:00\n-- actor: claude-code\n-  id: 01kxntqkec7n87hbykk45k3ym6\n-  text: |-\n-    Addressed all 23 findings from the 2026-07-16 10:11 review round:\n-\n-    1. Extracted `ProcessLanguageServerConnection.awaitResponse(id:)`'s 4-level closure nesting (withThrowingTaskGroup -> group.addTask -> withCheckedThrowingContinuation -> body) into two private static helpers, mirroring the ServerInstaller.scheduleTimeout/installTerminationHandler split: `awaitRegisteredResponse(id:pendingRequests:)` wraps the continuation-registration side, `failAfterTimeout(id:timeout:clock:pendingRequests:)` wraps the sleep-then-resolve-or-cancel side. `awaitResponse` itself is now a linear task-group setup with two single-call `group.addTask` bodies.\n-\n-    2. Added a one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (initialize through pullDiagnostics), matching the file's house style for simple passthroughs -- a concise summary sentence, no `- Parameter`/`- Throws` boilerplate since the full contract already lives on `LanguageServerConnection`'s protocol declarations in LanguageServerConnection.swift.\n-\n-    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly.\n-\n-    All 23 findings checked off on the task. Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNV9T5H6QRYMF61JTG057ZD","timestamp":"2026-07-16T16:14:06.001601Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -96,7 +96,7 @@\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -96,7 +96,7 @@\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNVBWMYBGCT492NE7YN21G5","timestamp":"2026-07-16T16:15:14.078490Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -100,4 +100,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -100,4 +100,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNVD0M3X50JT0TB3CAMVV24","timestamp":"2026-07-16T16:15:50.915811Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -96,7 +96,7 @@\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -96,7 +96,7 @@\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNVK9HEVYKMN2G8214TKR2J","timestamp":"2026-07-16T16:19:16.654773Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -100,4 +100,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\\n\\n  **Resolved:** Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above `public func shutdown() async throws`, matching the house style of its neighboring one-line doc comments (`initialize`, `initialized`, `exit`, etc.) established in the prior 2026-07-16 10:11 fix round. Verified: `swift build` clean (exit 0); `swift test` (full suite) → 511/511 tests in 44 suites passing.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -100,4 +100,4 @@\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n-## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\\n\\n  **Resolved:** Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above `public func shutdown() async throws`, matching the house style of its neighboring one-line doc comments (`initialize`, `initialized`, `exit`, etc.) established in the prior 2026-07-16 10:11 fix round. Verified: `swift build` clean (exit 0); `swift test` (full suite) → 511/511 tests in 44 suites passing.\n\\ No newline at end of file\n+## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [ ] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXNVKG7CMKKP6M21Q7R6EYSR","timestamp":"2026-07-16T16:19:23.500896Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -94,6 +94,15 @@\n\n     All 23 findings checked off on the task. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n+- actor: claude-code\n+  id: 01kxnvkg7b1hyv7cgyrwb6pc3t\n+  text: |-\n+    Addressed the 2026-07-16 11:06 review finding: `public func shutdown()` in Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift was missing its `///` doc comment (the one gap left in the prior 10:11 fix round, which covered `initialize` through `pullDiagnostics` but skipped `shutdown` itself). Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above the method, matching the one-line house style of its neighbors.\n+\n+    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing. Both findings checked off on the task. Adversarial double-check launched via really-done to confirm the doc comment is accurate/scoped and no other public declaration in the file was missed; awaiting its verdict before final handoff.\n+\n+    Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T16:19:23.499152+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -94,15 +94,6 @@\n\n     All 23 findings checked off on the task. Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T16:04:09.292981+00:00\n-- actor: claude-code\n-  id: 01kxnvkg7b1hyv7cgyrwb6pc3t\n-  text: |-\n-    Addressed the 2026-07-16 11:06 review finding: `public func shutdown()` in Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift was missing its `///` doc comment (the one gap left in the prior 10:11 fix round, which covered `initialize` through `pullDiagnostics` but skipped `shutdown` itself). Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above the method, matching the one-line house style of its neighbors.\n-\n-    Verification: `swift build` clean (exit 0). `swift test` (full suite, no filter) -> 511/511 tests in 44 suites passing. Both findings checked off on the task. Adversarial double-check launched via really-done to confirm the doc comment is accurate/scoped and no other public declaration in the file was missed; awaiting its verdict before final handoff.\n-\n-    Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T16:19:23.499152+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNVNX676D985RKAA2C4BKQW","timestamp":"2026-07-16T16:20:42.311272Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -103,6 +103,13 @@\n\n     Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T16:19:23.499152+00:00\n+- actor: claude-code\n+  id: 01kxnvnx66ersfdc5gxvavjpy9\n+  text: |-\n+    Adversarial double-check (via really-done) returned REVISE, but on inspection its one finding was already stale: it flagged the two 2026-07-16 11:06 findings as still unchecked with no Resolved note in .kanban/tasks/01KXKY4J6DZ6M7ETN54TZ81EAW.md — it had read that file's cache before my prior `update task` call (which checked both boxes and appended a Resolved note) had been re-rendered to disk. Re-confirmed via a fresh `get task` and a fresh grep of the .md file: both findings are `[x]` with the Resolved note present, consistent with every earlier round. Its secondary note (an unrelated one-line `.github/workflows/ci.yml` diff) is the same pre-existing, out-of-scope change already logged and deliberately left untouched per the 2026-07-16 15:07 comment. No further changes needed.\n+\n+    Final state: `swift build` clean (exit 0); `swift test` -> 511/511 tests in 44 suites passing. Task is green. Leaving in doing, ready for /review.\n+  timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -103,13 +103,6 @@\n\n     Leaving in doing, ready for /review.\n   timestamp: 2026-07-16T16:19:23.499152+00:00\n-- actor: claude-code\n-  id: 01kxnvnx66ersfdc5gxvavjpy9\n-  text: |-\n-    Adversarial double-check (via really-done) returned REVISE, but on inspection its one finding was already stale: it flagged the two 2026-07-16 11:06 findings as still unchecked with no Resolved note in .kanban/tasks/01KXKY4J6DZ6M7ETN54TZ81EAW.md — it had read that file's cache before my prior `update task` call (which checked both boxes and appended a Resolved note) had been re-rendered to disk. Re-confirmed via a fresh `get task` and a fresh grep of the .md file: both findings are `[x]` with the Resolved note present, consistent with every earlier round. Its secondary note (an unrelated one-line `.github/workflows/ci.yml` diff) is the same pre-existing, out-of-scope change already logged and deliberately left untouched per the 2026-07-16 15:07 comment. No further changes needed.\n-\n-    Final state: `swift build` clean (exit 0); `swift test` -> 511/511 tests in 44 suites passing. Task is green. Leaving in doing, ready for /review.\n-  timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n position_column: doing\n","transaction_id":null}
{"id":"01KXNW1XCRFZ5BGD7Q3QXY3SDF","timestamp":"2026-07-16T16:27:15.736625Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -112,7 +112,7 @@\n   timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -112,7 +112,7 @@\n   timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n","transaction_id":null}
{"id":"01KXNW218NS84QR2PBCE08QS9F","timestamp":"2026-07-16T16:27:19.701740Z","op":"update","item_id":"01KXKY4J6DZ6M7ETN54TZ81EAW","forward_patch":"--- original\n+++ modified\n@@ -112,8 +112,8 @@\n   timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: af80\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n ## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\\n\\n  **Resolved:** Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above `public func shutdown() async throws`, matching the house style of its neighboring one-line doc comments (`initialize`, `initialized`, `exit`, etc.) established in the prior 2026-07-16 10:11 fix round. Verified: `swift build` clean (exit 0); `swift test` (full suite) → 511/511 tests in 44 suites passing.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -112,8 +112,8 @@\n   timestamp: 2026-07-16T16:20:42.310464+00:00\n depends_on:\n - 01KXKY3XWKM1TRR9X0F4SQ0SF0\n-position_column: done\n-position_ordinal: af80\n+position_column: review\n+position_ordinal: '80'\n title: Add ServerInstaller engine with injectable process runner and LspAutoInstall policy\n ---\n ## What\\nCreate `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift`:\\n\\n- `public struct LspAutoInstall: Sendable` — the opt-out policy: `isEnabled: Bool = true` (on by default, per agreed design), `timeout: Duration = .seconds(300)`. Doc-comment that installs run the ecosystem's native global installer (npm -g, rustup component, go install, pipx, brew) on the user's machine and how to opt out.\\n- **Extract `LSPDaemon.isExecutableOnPath` into a shared internal helper** (e.g. `BinaryLookup` in this file or a small sibling) that both `LSPDaemon` and `ServerInstaller` consume — the downstream task extending lookup with `extraSearchDirectories` builds on this same helper, so this extraction must land here first (task 3 depends on this task for exactly this reason).\\n- `protocol InstallRunner: Sendable` — the test seam, mirroring how `ConnectionFactory` decouples `LSPDaemon` from real processes: `func run(tool: String, arguments: [String], timeout: Duration) async throws -> InstallRunResult` where `InstallRunResult` carries `exitCode: Int32` and a bounded `output: String` tail (stdout+stderr combined) for error reporting.\\n- `struct ProcessInstallRunner: InstallRunner` — production implementation on Foundation `Process` (spawn via `/usr/bin/env <tool> <args>`, matching `ProcessLanguageServerConnection`'s PATH-resolution approach). Must terminate the child in BOTH exit paths: kills on timeout throwing `CodeContextError.timeout`, AND kills on **task cancellation** via `withTaskCancellationHandler` — a production `shutdown()` during a real brew/npm run must not block for up to the 300 s timeout.\\n- `actor ServerInstaller` — orchestrates: `func install(spec: ServerSpec) async -> Bool` (true = install command succeeded):\\n  - returns false immediately when `spec.installer == nil`, when the policy is disabled, or when the installer `tool` isn't on `$PATH` (via the shared lookup helper)\\n  - **at-most-once per `spec.command` per instance** — records attempted commands and never retries a completed or in-flight attempt (concurrent callers for the same command await the same in-flight result); this is the backstop against install loops. Note the cancellation semantics of the shared in-flight `Task`: a caller awaiting a shared task's value being cancelled does not cancel the underlying install — the runner-level cancellation handler above is what makes shutdown prompt.\\n  - logs start/success/failure with the captured output tail via `Log.lsp`\\n\\n## Acceptance Criteria\\n- [ ] Disabled policy, nil installer, or missing installer tool → no runner invocation, returns false\\n- [ ] Exit 0 → true; nonzero exit → false with output tail logged; runner throw/timeout → false\\n- [ ] Same command installed twice (sequentially or concurrently) invokes the runner exactly once\\n- [ ] `ProcessInstallRunner` enforces its timeout AND terminates its child promptly when the running task is cancelled\\n- [ ] `isExecutableOnPath` logic lives in one shared helper consumed by both `LSPDaemon` and `ServerInstaller`\\n\\n## Tests\\n- [ ] `Tests/FoundationModelsCodeContextTests/ServerInstallerTests.swift` with a `FakeInstallRunner` (in Tests Support/, alongside the existing fakes): success, nonzero-exit, throwing runner, policy-disabled, nil-installer, tool-missing, and at-most-once/concurrent-dedupe cases\\n- [ ] `ProcessInstallRunner` integration cases using harmless real executables — `true` (exit 0), `false` (exit 1), `sleep 60` with a sub-second timeout asserting `CodeContextError.timeout`, and `sleep 60` cancelled mid-run asserting prompt return (bounded wall-clock) — no network, CI-safe\\n- [ ] `swift test --filter ServerInstallerTests` passes\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-16 08:40)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:75` — InstallOutputTailBuffer reimplements existing bounded tail-buffer logic rather than reusing or extending ProcessLanguageServerConnection's StderrTailBuffer. The doc comment explicitly states 'Mirrors `ProcessLanguageServerConnection`'s `StderrTailBuffer`', but no explanation is given for why the existing implementation wasn't reused. Evaluate whether ProcessLanguageServerConnection.StderrTailBuffer can be reused directly (if the API fits), or whether a shared generic BoundedTailBuffer abstraction should be extracted and used by both implementations to avoid maintaining two copies.\\n\\n  **Resolved:** Extracted a shared `BoundedTailBuffer` (`Sources/FoundationModelsCodeContext/LSP/BoundedTailBuffer.swift`), parameterized by `maxChunks: Int`. `ProcessLanguageServerConnection`'s former private `StderrTailBuffer` (capped at 20) and `ServerInstaller`'s former private `InstallOutputTailBuffer` (capped at 40) are both deleted; `ProcessLanguageServerConnection.stderrTailBuffer` now inits `BoundedTailBuffer(maxChunks: 20)` and `ProcessInstallRunner.run` inits `BoundedTailBuffer(maxChunks: 40)`. Updated stale cross-reference doc comments in `Index/Store.swift` and `CodeContextStateTests.swift` that named the old `StderrTailBuffer` type.\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:123` — ResumeGuard duplicates the idempotent-resume synchronization logic that already exists in PendingRequestTable. The doc comment explicitly states 'This is the same idempotent-resume guarantee, specialized to a single continuation instead of a table keyed by request id', indicating the core synchronization pattern is being reimplemented rather than extracted. Extract the core idempotent-resume synchronization logic into a generic abstraction that can be reused by both PendingRequestTable and ResumeGuard, rather than maintaining two copies of the same synchronization pattern. At minimum, add a cross-reference comment explaining why the existing PendingRequestTable pattern was not extended.\\n\\n  **Resolved:** Extracted a shared generic `ResumeOnce<Value: Sendable>` (`Sources/FoundationModelsCodeContext/LSP/ResumeOnce.swift`) providing `resume(with:) -> Bool`, `resume(returning:)`, and `resume(throwing:)` over a lock+hasResumed flag. `ServerInstaller`'s former private `ResumeGuard` is deleted; `ProcessInstallRunner.run` now constructs `ResumeOnce(continuation:)` directly. `PendingRequestTable` (`ProcessLanguageServerConnection.swift`) was also refactored to store `[Int: ResumeOnce<Data>]` instead of raw `CheckedContinuation`s, routing every resume (`register`/`resolve`/`resolveOrBuffer`/`failAll`) through the same primitive rather than calling `continuation.resume(...)` directly at each call site — both types now share one idempotent-resume implementation instead of two hand-rolled copies.\\n\\n  Verified: `swift build` clean; `swift test` → 511/511 tests in 44 suites passing (including `PendingRequestTableTests` and `ServerInstallerTests`/`ProcessInstallRunnerTests`, unchanged behavior).\\n\\n## Review Findings (2026-07-16 09:09)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:97` — The `run(tool:arguments:timeout:)` method has 4+ levels of nesting with complex async/await composition and race-condition handling, making the control flow difficult to follow. Multiple independent completion paths (timeout task, process termination handler, cancellation handler) coordinate through shared `resumeGuard` state. Extract the timeout-and-termination-handler coordination into a separate helper function that returns a single `ResumeOnce` after setup, reducing the nesting and making the race explicitly named. Alternatively, use a simpler pattern like `AsyncStream` or refactor to use `withTimeout` if available.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:350` — The low-level POSIX read(2) loop with EINTR error handling is duplicated across files. `ProcessInstallRunner.drainOutput` re-implements the identical errno/EINTR handling, buffer setup, and read loop that `ProcessLanguageServerConnection.readChunk` already encapsulates. Two blocks that differ only in variable names (chunkSize vs readChunkSize) and the action on bytesRead > 0 should be extracted into a single shared function. Extract the raw read(2) handling into a shared utility function—e.g., `ProcessUtilities.readChunkWithInterruptHandling(from:bufferSize:) -> Data?`—in a new or existing LSP utilities module, then have `ProcessInstallRunner.drainOutput` call it in a loop (mirroring how `ProcessLanguageServerConnection.readChunk` uses `readChunk`), eliminating the duplicated logic.\\n\\n## Review Findings (2026-07-16 09:34)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:164` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ServerInstaller.swift line 192. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:428` — runStderrDrainLoop is nearly identical to drainOutput in ServerInstaller.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:121` — awaitCompletion has 4 levels of closure nesting (withTaskCancellationHandler → withCheckedThrowingContinuation → process.terminationHandler → Task), making it difficult to track variable scope and control flow. The function must coordinate three independent completion paths (process termination, timeout, cancellation) that interact through ResumeOnce, increasing cognitive load. Extract the timeout task and terminationHandler setup into separate helper functions (e.g., configureTimeoutTask, configureTerminationHandler) to reduce nesting depth to 2-3 levels and isolate the coordination logic.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:192` — The file path URL `/usr/bin/env` is hardcoded here and also hardcoded in ProcessLanguageServerConnection.swift line 164. Both files spawn processes using the same path and should coordinate through a shared constant — if this path ever changes (for testing, mocking, or alternate systems), it must change in two places. Extract \\\"/usr/bin/env\\\" to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller so the path changes in one place.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:206` — drainOutput is nearly identical to runStderrDrainLoop in ProcessLanguageServerConnection.swift — both implement the same read-decode-append loop that differs only in logging and the isEmpty check. These blocks should be consolidated into a shared function. Extract a shared function in ProcessUtilities (e.g., drainFileDescriptor(fileDescriptor:into:bufferSize:shouldLog:minLength:)) parameterized by logging and filter behavior, eliminating the duplicate read/decode/append algorithm.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ServerInstaller.swift:208` — The buffer size literal 65536 appears in two files for the same purpose (reading from pipe file descriptors in a loop) but is not coordinated through a shared constant. ProcessLanguageServerConnection defines readChunkSize = 65536 and uses it consistently, while ServerInstaller hardcodes the value. If this buffer size changes for performance reasons, it must change in two places. Extract the buffer size to a shared constant (e.g., in ProcessUtilities.swift or a configuration module) and use it in both ProcessLanguageServerConnection and ServerInstaller, so the value changes in one place.\\n\\n  **Resolved (all 4 findings, root-cause pass):** Added two shared constants to `Sources/FoundationModelsCodeContext/LSP/ProcessUtilities.swift`: `envExecutablePath = \\\"/usr/bin/env\\\"` and `defaultChunkSize = 65536`. Both `ProcessLanguageServerConnection`'s `init` and `ServerInstaller.swift`'s `ProcessInstallRunner.run` now build `Process.executableURL` from `ProcessUtilities.envExecutablePath`; `runReaderLoop` and `runStderrDrainLoop` now pass `ProcessUtilities.defaultChunkSize` instead of the old private `readChunkSize` constant (deleted). Extracted the full read-decode-append *loop* (not just the raw chunk read) into a new `ProcessUtilities.drainChunks(from:bufferSize:onChunk:)`, which loops on `readChunk` until EOF, decodes UTF-8, skips empty/undecodable chunks, and invokes a per-chunk closure — `ProcessLanguageServerConnection.runStderrDrainLoop` now calls it with a closure that logs then appends, and `ServerInstaller`'s `ProcessInstallRunner.drainOutput` calls it with a closure that only appends; both former hand-rolled `while let chunk = ...` loops are gone. Restructured `ProcessInstallRunner.awaitCompletion` by extracting two new private static helpers, `scheduleTimeout(pid:timeout:clock:resumeGuard:) -> Task<Void, Never>` and `installTerminationHandler(on:timeoutTask:drainTask:tailBuffer:resumeGuard:)`; `awaitCompletion`'s continuation body now just constructs `resumeGuard` and calls those two helpers, so its own nesting is `withTaskCancellationHandler` → continuation (2 levels) instead of 4, with the timeout-task and termination-handler setup each living in their own shallow function.\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-refactor baseline exactly (pure refactor, no behavior change). Confirmed no remaining `/usr/bin/env` or `65536` literals in `ServerInstaller.swift` or `ProcessLanguageServerConnection.swift` (grepped both files); a third, unrelated file (`Diagnostics/DiagnosticsScope.swift`, a `git` subprocess helper) still hardcodes `/usr/bin/env` independently but was out of scope for this review round (not cited in the findings, not part of the LSP process-spawning pair these findings target) and was left untouched to avoid scope creep.\\n\\n## Review Findings (2026-07-16 10:11)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:390` — The awaitResponse function has 4+ levels of nested closures (withThrowingTaskGroup → group.addTask → withCheckedThrowingContinuation → continuation body), making it difficult to read and reason about the logic. Extract the request-registration and timeout-scheduling logic into separate helper functions (similar to scheduleTimeout and installTerminationHandler in ServerInstaller) to reduce the nesting depth to 3 levels or less, improving readability and maintainability.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:418` — public func initialize lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialize.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:423` — public func initialized lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing initialized.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:431` — public func exit lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing exit.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:435` — public func didOpen lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didOpen.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:440` — public func didChange lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didChange.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:447` — public func didSave lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didSave.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:451` — public func didClose lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing didClose.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:455` — public func documentSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing documentSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:459` — public func definition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing definition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:463` — public func typeDefinition lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing typeDefinition.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:467` — public func hover lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing hover.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:471` — public func references lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing references.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:477` — public func implementations lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing implementations.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:481` — public func prepareCallHierarchy lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareCallHierarchy.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:485` — public func outgoingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing outgoingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:489` — public func incomingCalls lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing incomingCalls.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:493` — public func prepareRename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing prepareRename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:497` — public func rename lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing rename.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:501` — public func codeActions lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing codeActions.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:507` — public func resolveCodeAction lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing resolveCodeAction.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:511` — public func workspaceSymbols lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing workspaceSymbols.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:515` — public func pullDiagnostics lacks a doc comment; every public declaration must carry ///. Add /// doc comment describing pullDiagnostics.\\n\\n  **Resolved (all 23 findings):** Extracted `awaitResponse(id:)`'s race into two new private static helpers mirroring `ProcessInstallRunner`'s `scheduleTimeout`/`installTerminationHandler` split: `awaitRegisteredResponse(id:pendingRequests:)` (the withCheckedThrowingContinuation registration side) and `failAfterTimeout(id:timeout:clock:pendingRequests:)` (the sleep-then-resolve-or-cancel side). `awaitResponse` itself is now a linear `withThrowingTaskGroup` with two single-call `group.addTask` bodies instead of inline closures, cutting its own nesting to `withThrowingTaskGroup` → `group.addTask` → helper call (no further nesting inside either branch). Added a concise one-line `///` doc comment to each of the 22 flagged public LSP protocol-conformance methods (`initialize` through `pullDiagnostics`), matching the file's existing house style for simple passthroughs (single-sentence summary, no `- Parameter`/`- Throws` boilerplate since the full contract is already documented on `LanguageServerConnection`'s protocol declarations).\\n\\n  Verified: `swift build` clean (exit 0). `swift test` (full suite, no filter) → 511/511 tests in 44 suites passing, matching the pre-existing baseline exactly (pure refactor + doc-only additions, no behavior change).\\n\\n## Review Findings (2026-07-16 11:06)\\n\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:230` — Public method `shutdown()` lacks documentation comment, while all other public protocol-conformance methods in this section have doc comments. Add a doc comment before the method, e.g., `/// Sends the shutdown request to gracefully terminate the language server.`.\\n- [x] `Sources/FoundationModelsCodeContext/LSP/ProcessLanguageServerConnection.swift:269` — Every public declaration must carry a `///` doc comment. `shutdown()` is public but lacks one. Add a one-line doc comment before the method, e.g., `/// Sends the `shutdown` request to begin server shutdown.`.\\n\\n  **Resolved:** Added `/// Sends the `shutdown` request that begins graceful server termination.` directly above `public func shutdown() async throws`, matching the house style of its neighboring one-line doc comments (`initialize`, `initialized`, `exit`, etc.) established in the prior 2026-07-16 10:11 fix round. Verified: `swift build` clean (exit 0); `swift test` (full suite) → 511/511 tests in 44 suites passing.\n\\ No newline at end of file\n","transaction_id":null}
